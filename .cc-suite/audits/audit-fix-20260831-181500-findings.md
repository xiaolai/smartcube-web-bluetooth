# Audit Findings

**Run**: audit-fix 20260831-181500 | **Scope**: branch changes (integration vs upstream/main, 37 code files) | **Audit type**: mini
**Model**: gpt-5.6-sol | **Effort**: high | **Audit thread**: per-file jobs — thread IDs in /tmp/auditfix-results.ndjson
**Status values**: open | fixed | not-fixed | partial | regressed | skipped (severity filter) | skipped (user stop)
**Round 1 verification**: independent Codex pass (gpt-5.6-sol, read-only, 9 batches, all completed); statuses below are its verdicts. Finding #16 was verdicted REGRESSED (a fix of this round made a throwing validator poison the notification chain) and was re-fixed with a red-green test after verification — its status is recorded as fixed. `not-fixed` rows are predominantly deliberate: documented legacy-API contracts, field-tested firmware workarounds kept pending hardware evidence, and declined pure-structure refactors; the verifier's notes acknowledge the documented rationales.

Note: 3 files (src/smartcube/index.ts, src/smartcube/protocols/giiker.ts, src/smartcube/protocols/moyu32.ts) failed their first audit run (capacity/deadline) were retried successfully with 15-minute deadlines; their findings are rows 284+.

| # | File | Line | Severity | Dimension | Finding | Suggested fix | Status | Round |
|---|------|------|----------|-----------|---------|---------------|--------|-------|
| 1 | scripts/check-dist.mjs | 7 | Medium | Logic & Correctness | Loads hardcoded bundle paths instead of the package’s declared `exports` entry points, so broken import/require mappings in `package.json` can pass validation. | Import and require `smartcube-web-bluetooth` via Node self-reference, or test the packed package from a temporary consumer. | fixed | 1 |
| 2 | scripts/check-dist.mjs | 16 | Medium | Logic & Correctness | Removes `default` from both export sets, allowing a default export present in only one bundle to go undetected despite claiming API equivalence. | Compare `default` normally; only exclude the CommonJS metadata property `__esModule`. | fixed | 1 |
| 3 | scripts/check-dist.mjs | 10 | Low | Shortcuts & Patches | The expected API check hardcodes only 4 of the current 14 runtime exports, so a simultaneous omission from both bundles can silently remove any other public export. | Maintain a complete checked-in runtime API snapshot or manifest and validate both bundles against it. | fixed | 1 |
| 4 | scripts/check-dist.mjs | 12 | Low | Duplication | The ESM and CommonJS required-export checks duplicate the same assertion and error construction. | Iterate over named bundle descriptors such as `{ label, exports }` and apply one assertion loop. | fixed | 1 |
| 5 | src/gan-bit-reader.ts | 15 | Low | Logic & Correctness | `Uint8Array` inputs are retained by reference, so caller mutation changes subsequent reads and can invalidate the cached `bitLength`; array inputs are copied, creating inconsistent ownership semantics. | Copy all inputs with `new Uint8Array(message)`, or document that the reader is a live view and derive length from the view. | not-fixed | 1 |
| 6 | src/gan-bit-reader.ts | 32 | Medium | Logic & Correctness | `offset` and `bitLength` are not validated. Negative or fractional values reach bitwise indexing and can silently insert zero bits or read the wrong number of bits; zero/negative lengths are also accepted by the `<= 8` branch. | Require a nonnegative integer offset and allow only positive integer lengths `1..8`, `16`, or `32`; throw `RangeError` otherwise. | partial | 1 |
| 7 | src/gan-bit-reader.ts | 39 | Medium | Shortcuts & Patches | Out-of-bounds 16/32-bit reads silently coerce missing byte reads from `NaN` to zero, returning a plausible but fabricated value for truncated packets. This backward-compatibility behavior masks malformed input. | Reject reads whose complete range is unavailable; if zero-padding remains necessary, expose it as an explicit opt-in mode. | not-fixed | 1 |
| 8 | src/gan-bit-reader.ts | 37 | Low | Refactoring Debt | Every 16/32-bit read allocates both a `Uint8Array` and `DataView` despite this class being documented as hot-path code; gyro decoding consequently creates multiple short-lived objects per notification. | Assemble the result directly from byte values, reversing byte order when `littleEndian` is requested. | partial | 1 |
| 9 | src/gan-cube-definitions.ts | 5,48,51-60 | Medium | Logic & Correctness | Exported shared arrays remain mutable at runtime; `GAN_CIC_LIST` and `GAN_ENCRYPTION_KEYS` are also mutable at the type level. Consumer mutation can corrupt device discovery or encryption for subsequent connections. | Expose deeply frozen, readonly structures or keep mutable data private and return defensive copies. | fixed | 1 |
| 10 | src/gan-cube-definitions.ts | 8-11,53-58 | Low | Duplication | Four Gen1 table entries exactly duplicate the Gen2/3/4 and MoYu key/IV arrays, creating two sources of truth for security-sensitive bytes. | Extract named immutable key/IV constants and reference them from both tables. | fixed | 1 |
| 11 | src/gan-cube-encrypter.ts | 7 | Low | Shortcuts & Patches | `AesBlockCipher` falsely declares AES results as `number[]`, while `aes-js` returns `Uint8Array`; the double assertion at line 42 suppresses this mismatch and could permit invalid array operations later. | Use accurate `Uint8Array` signatures or cache the typed `ModeOfOperation.ecb` API. | fixed | 1 |
| 12 | src/gan-cube-encrypter.ts | 24 | Low | Refactoring Debt | `_key` is retained as mutable instance state although it is only needed during construction; `_iv` is likewise mutable despite being an invariant. | Build a local `saltedKey`, retain only a `private readonly iv`, and initialize the cipher from the local key. | fixed | 1 |
| 13 | src/gan-cube-encrypter.ts | 102 | Low | Logic & Correctness | Gen3 and Gen4 are value aliases of the Gen2 constructor, changing prior observable behavior: all constructors have the Gen2 name and `instanceof GanGen3CubeEncrypter` and `instanceof GanGen4CubeEncrypter` are indistinguishable. This does not fully preserve backward compatibility. | Restore named subclasses if constructor identity is supported, or remove the generation-specific compatibility aliases and use the shared implementation explicitly. | fixed | 1 |
| 14 | src/gan-cube-protocol.ts | 249 | Medium | Logic & Correctness | Initialization is not atomic: if `startNotifications()` rejects, both event listeners remain registered; if disconnection occurs while it is pending, `create()` can resolve with an already-closed connection. | Wrap startup in `try/catch`, remove listeners and stop notifications on failure, then verify the connection remains active before returning. | fixed | 1 |
| 15 | src/gan-cube-protocol.ts | 303 | Medium | Logic & Correctness | Disconnect completes the event stream without awaiting `notificationChain`; notifications accepted before disconnect can finish afterward and have their decoded events silently discarded. | Stop accepting notifications, drain the existing chain before completing the subject, and handle protocol-triggered disconnects without self-deadlocking. | partial | 1 |
| 16 | src/gan-cube-protocol.ts | 292 | Medium | Shortcuts & Patches | The blanket empty `catch` treats every failure as a corrupt frame, including programming errors in the encrypter, validator, or driver, causing silent event loss and making defects undiagnosable. | Catch only expected malformed-frame errors and expose, log, or propagate unexpected failures through an explicit error channel. | fixed | 1 |
| 17 | src/gan-cube-protocol.ts | 209 | Low | Duplication | Commands are serialized twice: this per-connection `GattWriteQueue` wraps `writeGattCharacteristicValue()`, which already uses a per-device `GattWriteQueue`. | Remove the local queue and call `writeGattCharacteristicValue()` directly. | fixed | 1 |
| 18 | src/gan-cube-protocol.ts | 199 | Low | Refactoring Debt | Core connection dependencies and the writable `Subject` are public and mutable, allowing callers to replace the device, characteristics, encrypter, driver, or event stream and violate connection invariants. | Make dependencies `private readonly`; retain a private subject and expose only `subject.asObservable()` through a readonly property. | partial | 1 |
| 19 | src/gan-cube-protocol.ts | 254 | Low | Shortcuts & Patches | A missing device name is reported as the fabricated identifier `"GAN-XXXX"`, making unavailable metadata indistinguishable from a real device name. | Return nullable/optional metadata or use an explicitly documented unknown value outside the identity field. | not-fixed | 1 |
| 20 | src/gan-cube-protocol.ts | 258 | Low | Shortcuts & Patches | A missing MAC address is reported as the valid-looking `"00:00:00:00:00:00"`, which can be cached or used as real identity/keying data. | Require a validated MAC when constructing classic connections or make `deviceMAC` nullable. | not-fixed | 1 |
| 21 | src/gan-cube-protocol.ts | 9 | Low | Duplication | Four one-property command aliases repeat the same structure and are used only to construct the union at line 29. | Replace them with `type GanCubeCommand = { type: "REQUEST_HARDWARE" \| "REQUEST_FACELETS" \| "REQUEST_BATTERY" \| "REQUEST_RESET" };`. | fixed | 1 |
| 22 | src/gan-driver-select.ts | 52 | Medium | Logic & Correctness | Making `events$` optional allows notifications emitted during `GanCubeClassicConnection.create()` to be lost before callers can subscribe; the SmartCube caller explicitly buffers this race, but the legacy caller does not. | Use a bounded `ReplaySubject` by default or buffer initialization events until the connection is returned. | not-fixed | 1 |
| 23 | src/gan-driver-select.ts | 55 | Low | Logic & Correctness | The MAC is parsed before confirming that a supported gen2/3/4 service exists, so an invalid MAC throws even though the documented no-service result is `null`. | Move salt parsing inside the matched-service branch. | fixed | 1 |
| 24 | src/gan-driver-select.ts | 64 | Medium | Shortcuts & Patches | Cryptographic-key selection relies solely on the nullable, case-sensitive Bluetooth device-name prefix `AiCube`; absent, changed, or differently cased names select the wrong key and yield a connection that silently drops every decrypted frame. | Identify the model from reliable advertisement/profile metadata, or try supported keys and select one only after validating decrypted traffic. | not-fixed | 1 |
| 25 | src/gan-driver-select.ts | 91 | Low | Logic & Correctness | Devices exposing multiple recognized services are silently assigned the first match, with undocumented gen2→gen3→gen4 priority; a compatibility service can therefore select the wrong protocol. | Detect ambiguous profiles and reject them, or define and test an intentional precedence based on validated traffic. | not-fixed | 1 |
| 26 | src/gan-driver-select.ts | 47 | Low | Refactoring Debt | The 64-line orchestration function embeds a 33-line setup table that is rebuilt on every call, making protocol metadata and selection logic harder to review independently. | Move immutable generation descriptors to module scope and make key selection accept the device as an argument. | fixed | 1 |
| 27 | src/gan-gen1.ts | 36 | Low | Logic & Correctness | Unknown firmware key-table indices silently fall back to table 0, potentially decrypting unsupported firmware with the wrong key instead of rejecting it. | Return `null` when the indexed key is absent. | fixed | 1 |
| 28 | src/gan-gen1.ts | 57 | Medium | Logic & Correctness | Gyro frames whose vector magnitude exceeds one produce a non-unit quaternion with `w = 0`; corrupt data is emitted as valid orientation. | Reject implausible components or normalize the completed quaternion before emission. | not-fixed | 1 |
| 29 | src/gan-gen1.ts | 58 | Medium | Logic & Correctness | The Gen1 sensor-axis transform is inconsistent with the established `[-rawY, rawZ, -rawX]` mapping; it currently returns `[rawX, rawZ, -rawY]`, rotating the reported orientation incorrectly. See the [clean-room GAN implementation](https://github.com/cubing/cubing.js/blob/main/src/cubing/bluetooth/smart-puzzle/gan.ts). | Return `{ x: -n1, y: n2, z: -n0, w }` and add axis-specific tests. | fixed | 1 |
| 30 | src/gan-gen1.ts | 64 | Medium | Logic & Correctness | Facelet parsing accepts arbitrary lengths and sticker codes 6–7; `charAt()` then inserts empty strings, allowing malformed payloads to become invalid FACELETS events. | Require exactly 18 bytes, validate every sticker is 0–5, and validate the resulting 54-character cube state. | partial | 1 |
| 31 | src/gan-gen1.ts | 136 | Low | Refactoring Debt | The 53-line factory combines firmware validation, key derivation, characteristic discovery, listener registration, notification startup, initial reads, and polling startup. | Extract firmware/key loading and characteristic acquisition into focused helpers. | not-fixed | 1 |
| 32 | src/gan-gen1.ts | 150 | Low | Shortcuts & Patches | Firmware validation uses opaque decimal magic values (`65543`, `16776704`, `65536`), obscuring the supported-version rule. | Introduce named hexadecimal constants such as `MIN_GEN1_FW` and `GEN1_FW_MASK`. | fixed | 1 |
| 33 | src/gan-gen1.ts | 165 | High | Logic & Correctness | Connection setup unconditionally requires and subscribes to FFF4, although established Gen1 profiles use FFF5 for gyro/physical state and commonly do not expose FFF4. Such cubes fail during `create()`; if FFF4 does exist, gyro data is also emitted again by the FFF5 poller. See the [GAN characteristic map](https://github.com/cubing/cubing.js/blob/main/src/cubing/bluetooth/smart-puzzle/gan.ts). | Remove the FFF4 dependency and obtain gyro data from FFF5, or feature-detect FFF4 and select exactly one gyro source. | fixed | 1 |
| 34 | src/gan-gen1.ts | 179 | Medium | Logic & Correctness | Failures after constructing the connection leave the device disconnect listener and characteristic listener installed; a rejected `startNotifications()` leaks a partially initialized connection. | Wrap post-construction initialization in `try/catch` and perform listener/notification teardown before rethrowing. | fixed | 1 |
| 35 | src/gan-gen1.ts | 195,226,237,264,282 | Low | Duplication | DataView slicing, Uint8Array construction, decryption, and error handling are repeated across every read path. | Add a `readDecrypted(characteristic, expectedLength)` helper with centralized validation. | fixed | 1 |
| 36 | src/gan-gen1.ts | 205 | Low | Duplication | Battery clamping, deduplication, and forced-emission state duplicate the shared event-bus policy, creating two independent deduplication state machines. | Keep battery policy in one layer; have this driver emit validated raw battery readings. | not-fixed | 1 |
| 37 | src/gan-gen1.ts | 229 | Medium | Logic & Correctness | Battery and facelet reads swallow every error, so initial setup and explicit commands resolve successfully without producing the requested event. | Let command/initialization reads reject; only scheduled background polling should catch and classify transient failures. | partial | 1 |
| 38 | src/gan-gen1.ts | 241 | Medium | Logic & Correctness | Every FACELETS event reports serial `0`, violating the circular move-counter contract once the cube has moved. | Read or pass the current FFF5 move counter and use it as the facelet serial. | fixed | 1 |
| 39 | src/gan-gen1.ts | 243 | High | Shortcuts & Patches | Every FACELETS event attaches a solved cubie state even when the decoded facelet string is scrambled; the event contains mutually contradictory state representations. | Convert the decoded facelets to actual CP/CO/EP/EO values or remove the unsupported field from the contract. | fixed | 1 |
| 40 | src/gan-gen1.ts | 260 | Medium | Refactoring Debt | `pollLoop()` is 65 lines and combines scheduling, gyro decoding, move recovery, timestamp decoding, periodic refreshes, retry policy, and event publication with nested control flow. | Split it into state decoding, move recovery, periodic-maintenance, and retry-scheduling helpers. | fixed | 1 |
| 41 | src/gan-gen1.ts | 263 | High | Logic & Correctness | FFF5 packets are not required to be 19 bytes before indexes 12–18 are read. A 16-byte decrypted packet can manufacture `U` moves from `undefined` values. | Require the protocol’s exact 19-byte physical-state frame before parsing anything. | partial | 1 |
| 42 | src/gan-gen1.ts | 278 | Medium | Logic & Correctness | Move bytes are not validated against the legal Gen1 codes; corrupt values can emit invented half turns or events with `face: -1`. | Validate against the supported move-code table before constructing notation. | fixed | 1 |
| 43 | src/gan-gen1.ts | 281 | High | Logic & Correctness | FFF6 timestamp packets are not length-checked even though indexes through 18 are accessed; short frames silently create zero or corrupted timing values. | Require a 19-byte timing frame before decoding its nine offsets. | partial | 1 |
| 44 | src/gan-gen1.ts | 302 | High | Logic & Correctness | Each FFF6 value is a per-move elapsed-time offset, but it is emitted directly as `cubeTimestamp`. Consequently timestamps move backward or repeat instead of representing an accumulated cube clock. The reference implementation accumulates these offsets. | Maintain an accumulated device clock, add offsets oldest-to-newest, and resynchronize it against host time when drift is excessive. | not-fixed | 1 |
| 45 | src/gan-gen1.ts | 303 | Medium | Logic & Correctness | All recovered moves receive the same host `localTimestamp`; older buffered moves were not locally observed at that time. | Set `localTimestamp` only for the newest move and use `null` for recovered earlier moves. | fixed | 1 |
| 46 | src/gan-gen1.ts | 308 | Medium | Logic & Correctness | `movePollTicks` counts successful polling iterations rather than moves, causing an unchanged facelet frame to be read and emitted roughly every 1.5 seconds. The intended reconciliation threshold is based on moves since the last check. | Increment a moves-since-check counter by the move-counter delta and refresh only after the chosen move threshold or an explicit request. | fixed | 1 |
| 47 | src/gan-gen1.ts | 313 | Low | Logic & Correctness | Battery timing assumes every poll represents exactly 30 ms, ignoring BLE-read duration, facelet reads, throttling, and retry backoff; the nominal 60-second interval can drift substantially. | Store a monotonic next-battery-read timestamp and compare it with `now()`. | fixed | 1 |
| 48 | src/gan-gen1.ts | 319 | Medium | Shortcuts & Patches | Poll failures are completely hidden and retried forever while the connection remains reported as healthy, so a wedged GATT session can silently stop producing data indefinitely. | Classify transient failures, expose/log repeated failures, and terminate the connection after a bounded threshold or confirmed GATT loss. | partial | 1 |
| 49 | src/gan-gen1.ts | 343 | Medium | Logic & Correctness | Explicit command reads can run concurrently with polling or teardown, causing overlapping GATT operations; because read errors are swallowed, the command may falsely report success. | Serialize all GATT operations through one queue and reject commands once teardown begins. | partial | 1 |
| 50 | src/gan-gen1.ts | 352 | Low | Shortcuts & Patches | Unsupported `REQUEST_HARDWARE` and `REQUEST_RESET` commands silently succeed through the default branch. | Explicitly reject unsupported commands so legacy callers can detect the capability mismatch. | fixed | 1 |
| 51 | src/gan-gen234-packet-validate.ts | 29 | Medium | Logic & Correctness | Gen2 FACELETS validation checks only permutation sums. Duplicate piece IDs, missing IDs, corner orientation value 3, and mismatched corner/edge permutation parity are accepted; an all-zero packet is incorrectly considered valid and reconstructs impossible piece IDs 28 and 66 downstream. | Reconstruct the final pieces, then validate complete CP/EP permutations, CO ranges, and permutation parity. | partial | 1 |
| 52 | src/gan-gen234-packet-validate.ts | 62 | Medium | Logic & Correctness | Gen3 accepts every non-MOVE opcode using only magic, opcode, and a nonzero `dataLength`. Malformed FACELETS, MOVE_HISTORY, HARDWARE, and BATTERY payloads pass validation, including the tested 16-byte FACELETS packet even though decoding reads through bit 131. | Add opcode-specific minimum sizes and semantic checks: legal cubie state, bounded history entries, printable hardware name, and battery range. | partial | 1 |
| 53 | src/gan-gen234-packet-validate.ts | 62 | Medium | Logic & Correctness | `dataLength` is never checked against `payload.length`. A forged large value can drive downstream history loops beyond available data; undersized packets produce truncated or `NaN` fields. | Require the declared data region to fit within the payload and enforce the expected length for each opcode. | partial | 1 |
| 54 | src/gan-gen234-packet-validate.ts | 79 | Medium | Logic & Correctness | Gen4 non-MOVE packets are accepted solely by opcode. Invalid FACELETS states, unbounded MOVE_HISTORY lengths, malformed hardware fields, and out-of-range or missing battery values therefore pass the trust-boundary check. | Dispatch to opcode-specific validators covering lengths and field invariants. | partial | 1 |
| 55 | src/gan-gen234-packet-validate.ts | 80 | Low | Logic & Correctness | Gen4 notifications can contain multiple 72-bit MOVE chunks, but only the first chunk’s face code is validated. | Iterate over every complete MOVE chunk and validate its opcode, face code, and direction; also validate trailing padding. | partial | 1 |
| 56 | src/gan-gen234-packet-validate.ts | 79 | Low | Dead Code | Opcode `251` (`0xFB`) is accepted, but the Gen4 driver has no handling case for it, so accepted packets produce no behavior. | Remove it from the allowlist or implement and document its decoding. | fixed | 1 |
| 57 | src/gan-gen234-packet-validate.ts | 66 | Low | Duplication | The same face-code lookup `[2, 32, 8, 1, 16, 4]` is duplicated at line 83. | Extract a shared immutable face-code constant/helper. | fixed | 1 |
| 58 | src/gan-gen234-packet-validate.ts | 9 | Low | Duplication | All three validators repeat the length guard, reader construction, `try/catch`, and boolean-return scaffold. | Extract a small common validation wrapper while retaining generation-specific checks. | not-fixed | 1 |
| 59 | src/gan-gen234-packet-validate.ts | 10 | Low | Dead Code | The falsy-payload guard is inconsistent with the non-nullable parameter type; the same mismatch occurs at lines 56 and 75. | Either include `null \| undefined` in the public input type or remove the unreachable checks. | not-fixed | 1 |
| 60 | src/gan-gen234-packet-validate.ts | 9 | Low | Refactoring Debt | `isValidGanGen2Packet` exceeds 40 lines and combines six packet formats in one conditional chain. | Split opcode-specific checks into named helpers and dispatch by event type. | partial | 1 |
| 61 | src/gan-protocol-drivers.ts | 29 | Medium | Logic & Correctness | Decoded permutations and orientations are never validated for uniqueness or valid ranges; malformed packets can expose impossible cube states or incorrect facelets. | Validate CP/EP as complete permutations and CO/EO ranges before returning the state. | fixed | 1 |
| 62 | src/gan-protocol-drivers.ts | 81 | Low | Refactoring Debt | `moveBuffer` accepts every `GanCubeEvent`, but eviction blindly casts entries to `GanCubeMoveEvent`, weakening type safety and requiring redundant runtime checks. | Type the buffer, `push`, and injection method as `GanCubeMoveEvent`. | fixed | 1 |
| 63 | src/gan-protocol-drivers.ts | 100 | Medium | Shortcuts & Patches | Every history-request failure is silently converted into success. A request triggered by a periodic facelet packet may never be retried if no later notification arrives, permanently losing moves. | Preserve a pending-retry state or return failure so the caller can retry with bounded backoff. | not-fixed | 1 |
| 64 | src/gan-protocol-drivers.ts | 113 | Medium | Logic & Correctness | A serial difference of zero enters the eviction branch, so duplicate live MOVE notifications are emitted as additional physical moves. | Explicitly discard `diff === 0`; only evict when `diff === 1`. | fixed | 1 |
| 65 | src/gan-protocol-drivers.ts | 170 | Medium | Shortcuts & Patches | Recovery is completely skipped when the facelet serial is zero. Missing moves at the 255→0 boundary remain absent unless a later event happens to initiate another recovery. | Split recovery into windows on either side of the wrap instead of abandoning the request. | not-fixed | 1 |
| 66 | src/gan-protocol-drivers.ts | 241 | Medium | Logic & Correctness | The zero-elapsed fallback uses `timestamp - lastMoveTimestamp`, although `lastMoveTimestamp` starts at zero and the same full gap can be applied repeatedly to multiple recovered moves. This can massively inflate `cubeTimestamp`. | Initialize a nullable baseline from the first state event and distribute/interpolate elapsed time for recovered moves. | partial | 1 |
| 67 | src/gan-protocol-drivers.ts | 353 | Medium | Duplication | Gen3 and Gen4 handlers duplicate MOVE, MOVE_HISTORY, FACELETS, battery, and disconnect processing with mostly offset changes, making protocol fixes easy to apply inconsistently. | Extract parameterized decoding helpers driven by per-generation packet-layout definitions. | not-fixed | 1 |
| 68 | src/gan-protocol-drivers.ts | 353 | Low | Refactoring Debt | This handler is approximately 135 lines with a large nested conditional dispatcher; the Gen2 and Gen4 handlers have the same problem. | Dispatch event types to small event-specific methods. | not-fixed | 1 |
| 69 | src/gan-protocol-drivers.ts | 364 | Medium | Logic & Correctness | Gen3 checks only that `dataLength > 0`; it never verifies event-specific payload length before fixed-offset reads or history iteration. Truncated or inconsistent frames can throw or be partially decoded. | Validate declared and actual lengths for every event type before reading fields. | partial | 1 |
| 70 | src/gan-protocol-drivers.ts | 372 | Medium | Logic & Correctness | Gen3 stores a 16-bit live MOVE serial, while history responses and all range arithmetic use modulo-256 serials; FACELETS repeats this at line 426. A nonzero upper byte causes inconsistent emitted serials and failed deduplication/recovery. | Normalize both live counters with `& 0xFF`, or consistently reconstruct full-width history serials. | fixed | 1 |
| 71 | src/gan-protocol-drivers.ts | 374 | Medium | Logic & Correctness | The two-bit direction is not restricted to 0 or 1. Values 2 and 3 produce no suffix and are silently reported as clockwise moves. | Reject unsupported directions before mutating history or emitting a move. | fixed | 1 |
| 72 | src/gan-protocol-drivers.ts | 431 | Medium | Logic & Correctness | Missed-move recovery requires `lastLocalTimestamp` to be non-null. If every MOVE notification since initialization was lost, it remains null and periodic FACELETS packets never request history. | Treat null as idle: recover when `lastLocalTimestamp == null \|\| timestamp - lastLocalTimestamp > 500`. | fixed | 1 |
| 73 | src/gan-protocol-drivers.ts | 521 | Medium | Logic & Correctness | `createCommandMessage` unexpectedly clears hardware assembly state merely while encoding a command. If the resulting command is not sent or its write fails, valid partial state has already been destroyed. | Keep command encoding pure and reset assembly only when a request is successfully sent or a new response sequence begins. | partial | 1 |
| 74 | src/gan-protocol-drivers.ts | 557 | Medium | Logic & Correctness | Gen4 trusts `dataLength` without checking it against `eventMessage.length`; history, hardware-name, and battery branches can read outside the declared packet and emit garbage or `NaN`. | Add event-specific declared/actual-length validation before decoding. | partial | 1 |
| 75 | src/gan-protocol-drivers.ts | 569 | Medium | Logic & Correctness | Gen4 stores a 16-bit live MOVE serial while the history buffer operates modulo 256; FACELETS repeats this at line 628. Recovered events can therefore use a different serial domain from live events. | Normalize all serials to eight bits or consistently extend history serials to the live counter width. | fixed | 1 |
| 76 | src/gan-protocol-drivers.ts | 572 | Medium | Logic & Correctness | Gen4 accepts reserved direction values 2 and 3 and silently renders them as clockwise moves. Later chunks are especially exposed because external validation checks only the first chunk. | Require direction 0 or 1 for every chunk before updating history. | fixed | 1 |
| 77 | src/gan-protocol-drivers.ts | 633 | Medium | Logic & Correctness | Gen4 has the same null-timestamp recovery hole as Gen3: if all MOVE notifications are lost, facelet-based recovery is never attempted. | Treat a null timestamp as an idle stream eligible for immediate recovery. | fixed | 1 |
| 78 | src/gan-protocol-drivers.ts | 678 | Low | Logic & Correctness | Once four hardware fields exist, every retransmitted hardware packet emits another identical HARDWARE event because `hardwareInfoEmitted` is set but not checked here. | Guard this branch with `!hardwareInfoEmitted`, while retaining the deliberate gyro-capability update path. | fixed | 1 |
| 79 | src/gan-smart-cube.ts | 46 | High | Logic & Correctness | `requestDevice()` omits `optionalManufacturerData`, so `watchAdvertisements()` may not expose the manufacturer bytes required for automatic MAC discovery; default Gen2–4 connections can consequently fail without a custom provider. The [Web Bluetooth specification](https://webbluetoothcg.github.io/web-bluetooth/) grants this access through `optionalManufacturerData`. | Add `optionalManufacturerData: def.GAN_CIC_LIST`, preferably through the shared picker-options builder. | fixed | 1 |
| 80 | src/gan-smart-cube.ts | 62 | Medium | Logic & Correctness | GATT is connected before automatic MAC discovery at line 72. Connectable peripherals commonly stop advertising after connection, causing a five-second timeout and failed MAC resolution. | Capture advertisement data before connecting, then establish GATT. | fixed | 1 |
| 81 | src/gan-smart-cube.ts | 62 | Medium | Logic & Correctness | Any rejection after GATT connects—including service discovery, MAC-provider failure, invalid MAC, unsupported profile, or notification setup—leaves the device connected and may leave partially installed listeners. | Wrap post-connect initialization in `try/catch`; disconnect GATT on failure before rethrowing. | fixed | 1 |
| 82 | src/gan-smart-cube.ts | 62 | Low | Logic & Correctness | The non-null assertion on `device.gatt` suppresses a valid optional state from the Web Bluetooth typings and produces an opaque `TypeError` if GATT is unavailable. | Check `device.gatt` explicitly and throw a descriptive unsupported-device error. | fixed | 1 |
| 83 | src/gan-smart-cube.ts | 79 | Medium | Shortcuts & Patches | The function returns a classic connection without proving that the selected MAC decrypts valid cube traffic. A plausible but wrong MAC produces an apparently successful connection that silently drops every packet. | Request fresh state and wait for one validated decrypted event with a timeout; disconnect and reject if verification fails. | not-fixed | 1 |
| 84 | src/gan-smart-cube.ts | 28 | Low | Duplication | `MacAddressProvider` duplicates the identical definition in `src/smartcube/types.ts`, allowing the public and internal contracts to drift. | Define the type once in the shared types module and re-export/import it here. | fixed | 1 |
| 85 | src/gan-smart-cube.ts | 35 | Medium | Duplication | The device-selection, GATT-discovery, profile-selection, and MAC-resolution pipeline substantially duplicates `connectSmartCube`/`connectGanDevice`; it has already drifted by lacking advertisement permissions, pre-connect capture, caching, cancellation, verification, and failure cleanup. | Extract a shared “connect selected GAN device” routine and shared request-options construction, then adapt its result to the legacy API. | partial | 1 |
| 86 | src/gan-smart-cube.ts | 35 | Low | Refactoring Debt | This 51-line function combines picker configuration, connection lifecycle, service detection, address resolution, mutation, and protocol construction. | Split picker construction, MAC resolution, and connection initialization into focused helpers. | partial | 1 |
| 87 | src/gan-smart-cube.ts | 55 | Low | Duplication | The literal Device Information UUID exactly duplicates `def.GAN_GEN1_DEVICE_INFO_SERVICE` already present at line 51, creating a redundant permission entry. | Remove the duplicate and construct the service list through a deduplicating shared helper. | fixed | 1 |
| 88 | src/gan-smart-cube.ts | 56 | Low | Dead Code | Generic Access is granted as an optional service, but neither this connection path nor the returned `GanCubeConnection` API accesses it. | Remove the unused service grant unless a documented browser workaround requires it. | fixed | 1 |
| 89 | src/gan-smart-cube.ts | 71 | Low | Refactoring Debt | The awaited `&&`/` | \|` chain mixes control flow and value selection across three calls, relying on operator precedence and truthiness. This is difficult to extend safely. \| Replace it with a sequential `resolveMacAddress` helper using explicit `if (!mac)` steps. | fixed | 1 |
| 90 | src/gan-smart-timer.ts | 125 | Medium | Logic & Correctness | Frame validation does not enforce the protocol’s minimum/declared length; a short CRC-valid STOPPED frame reaches `makeTimeFromRaw` and throws an uncaught RangeError, while malformed frames with inconsistent length bytes can be accepted | Require the expected frame length, verify byte 1 matches `byteLength`, and validate payload size before decoding | partial | 1 |
| 91 | src/gan-smart-timer.ts | 129 | Medium | Logic & Correctness | CRC calculation ignores `DataView.byteOffset`; when the view covers a subsection of a larger buffer, CRC is calculated over unrelated bytes and valid notifications are dropped | Calculate CRC using view-relative bytes starting at `data.byteOffset + 2`, preferably by passing a bounded DataView/Uint8Array to the CRC function | fixed | 1 |
| 92 | src/gan-smart-timer.ts | 141 | Low | Logic & Correctness | Any byte is cast into `GanTimerState`, so CRC-valid frames can emit undocumented states such as 255 and violate the event API contract | Reject state values outside the defined enum before constructing or emitting the event | fixed | 1 |
| 93 | src/gan-smart-timer.ts | 168 | Medium | Logic & Correctness | Any failure after `gatt.connect()`—service discovery, characteristic discovery, or notification startup—rejects without disconnecting the GATT server; notification startup failure also leaves the registered listener attached | Wrap post-connect initialization in try/catch and remove attached listeners, stop notifications best-effort, and disconnect the server before rethrowing | partial | 1 |
| 94 | src/gan-smart-timer.ts | 176 | Medium | Logic & Correctness | Notifications start before callers can subscribe to the plain `Subject`; events arriving between `startNotifications()` and subscription are irretrievably lost | Buffer pre-subscription events with an appropriate replay/queue strategy, or redesign connection startup so a subscriber is installed before notifications are enabled | not-fixed | 1 |
| 95 | src/gan-smart-timer.ts | 202 | Medium | Logic & Correctness | `disconnectAction` is asynchronous but is exposed through `disconnect(): void`; callers cannot await completion, concurrent calls can start duplicate teardown operations, and a hanging `stopNotifications()` prevents the actual GATT disconnect | Expose `disconnect(): Promise<void>`, memoize/guard teardown for idempotence, and place the server disconnect in a `finally` block or perform it before awaiting best-effort notification shutdown | partial | 1 |
| 96 | src/gan-smart-timer.ts | 205 | Low | Shortcuts & Patches | All `stopNotifications()` failures are silently discarded, making teardown failures impossible to diagnose | Preserve best-effort cleanup but report the failure through logging, an error callback, or the returned disconnect promise | not-fixed | 1 |
| 97 | src/gan-smart-timer.ts | 75 | Low | Logic & Correctness | The exported time constructors accept negative, non-finite, fractional, or out-of-range components and can produce invalid timestamps and strings such as negative milliseconds | Define and enforce the input contract, rejecting non-finite/negative values and validating or normalizing seconds and milliseconds | fixed | 1 |
| 98 | src/gan-smart-timer.ts | 5 | Low | Duplication | The full Bluetooth base UUID suffix is repeated across all three constants and duplicated elsewhere in the repository | Centralize 16-bit Bluetooth UUID expansion in a shared helper or shared constants module | partial | 1 |
| 99 | src/gan-smart-timer.ts | 153 | Low | Refactoring Debt | `connectGanTimer` spans 68 lines and owns device selection, GATT setup, packet dispatch, recorded-time reads, and teardown, which obscures lifecycle invariants and contributed to missing cleanup paths | Extract device/GATT initialization, notification decoding, recorded-time reading, and idempotent teardown into focused helpers or a connection class | not-fixed | 1 |
| 100 | src/smartcube/attachment/address-hints.ts | 146 | High | Logic & Correctness | Raw `DataView` parsing ignores `byteOffset` and `byteLength`, so a valid subview can read unrelated backing-buffer bytes and derive the wrong MAC. | Slice relative to the view: from `byteOffset + 2` to `Math.min(byteOffset + byteLength, byteOffset + 11)`. | fixed | 1 |
| 101 | src/smartcube/attachment/address-hints.ts | 150 | High | Logic & Correctness | The manufacturer-map path also slices from backing-buffer offset zero instead of the mapped `DataView`’s bounds, potentially producing a wrong MAC and breaking GAN encryption. | Copy from `value.byteOffset` through `value.byteOffset + Math.min(value.byteLength, 9)`. | fixed | 1 |
| 102 | src/smartcube/attachment/address-hints.ts | 120 | Medium | Logic & Correctness | The function resolves on the first non-empty manufacturer-data map, contradicting its claim to merge packets until timeout. An unrelated first entry prevents a later MAC-bearing advertisement from being observed. | Accept a predicate/expected company IDs and resolve only when useful data appears, or continue accumulating until timeout/a bounded settle period; add a two-nonempty-frame test. | partial | 1 |
| 103 | src/smartcube/attachment/address-hints.ts | 149 | Medium | Logic & Correctness | GAN parsing stops at the first recognized company ID even when its payload is malformed or too short, so a later valid GAN entry is ignored. | Validate each candidate before returning it and continue scanning when a candidate cannot yield a MAC. | fixed | 1 |
| 104 | src/smartcube/attachment/address-hints.ts | 135 | Low | Logic & Correctness | A synchronous throw from `watchAdvertisements` bypasses the attached `.catch()`, rejects the outer promise unexpectedly, and leaves the timer and event listener installed. | Wrap the method invocation in `try/catch` and route both synchronous throws and promise rejections through `finish(null)`. | fixed | 1 |
| 105 | src/smartcube/attachment/address-hints.ts | 5 | Low | Duplication | The three cache operations repeat the same availability check, key construction, and exception-swallowing structure. | Extract a cache-key helper and a small guarded-storage operation helper. | not-fixed | 1 |
| 106 | src/smartcube/attachment/address-hints.ts | 73 | Low | Refactoring Debt | `waitForManufacturerData` spans 68 lines and combines policy selection, scan startup, event accumulation, abort handling, timeout handling, and cleanup. | Extract scan-policy calculation and listener/timer lifecycle management into focused helpers. | not-fixed | 1 |
| 107 | src/smartcube/attachment/address-hints.ts | 82 | Low | Shortcuts & Patches | Generic advertisement infrastructure contains a hardcoded `WCU_` product-name heuristic, coupling shared behavior to one protocol and making default behavior depend on naming conventions. | Move this policy to the MoYu caller or pass an explicit resolution policy from protocol metadata. | not-fixed | 1 |
| 108 | src/smartcube/attachment/address-hints.ts | 121 | Low | Refactoring Debt | `merged as unknown as BluetoothManufacturerData` uses an unnecessary double assertion even though `BluetoothManufacturerData` is already `Map<number, DataView>`, obscuring type correctness. The same occurs at line 130. | Return `merged` directly or introduce an explicit local alias if compatibility with alternate typings is required. | not-fixed | 1 |
| 109 | src/smartcube/attachment/gatt-snapshot.ts | 79 | High | Logic & Correctness | `getPrimaryServices()` is not raced against the abort signal. Aborting during stalled service discovery can leave the operation pending indefinitely without disconnecting, violating the documented cancellation contract. | Wrap service discovery in an abort-aware race, clean up its listener, and disconnect when aborted. | fixed | 1 |
| 110 | src/smartcube/attachment/gatt-snapshot.ts | 92 | Low | Logic & Correctness | Retry backoff ignores the abort signal, delaying cancellation by up to 300 ms despite promising rejection “as soon as the signal fires.” | Replace `delay` with an abortable delay that rejects with `AbortError`. | fixed | 1 |
| 111 | src/smartcube/attachment/gatt-snapshot.ts | 8-11 | Medium | Logic & Correctness | `disconnectGattSafe` invokes `disconnect()` before `Promise.resolve`, so a synchronous exception escapes instead of being suppressed. It can mask the original connection or discovery failure. | Put the invocation inside `try/catch`; optionally await its result to support nonstandard asynchronous implementations. | fixed | 1 |
| 112 | src/smartcube/attachment/gatt-snapshot.ts | 40-45,85-87 | Low | Duplication | Every connection failure triggers disconnection inside `connectGattWithTimeout` and then again in the caller’s catch block. | Give cleanup ownership to the outer retry loop and remove the duplicate inner calls. | fixed | 1 |
| 113 | src/smartcube/attachment/gatt-snapshot.ts | 45 | Low | Dead Code | `.catch(() => {})` is redundant because `disconnectGattSafe` already absorbs promise rejections, while it cannot catch the helper’s synchronous invocation failure. | Make `disconnectGattSafe` genuinely exception-safe and simply await it. | fixed | 1 |
| 114 | src/smartcube/attachment/gatt-snapshot.ts | 85-93 | Low | Shortcuts & Patches | The loop retries every non-abort exception even though its contract says retries are for transient failures. Deterministic permission, unsupported-operation, or data errors are retried and the final attempt may obscure the original failure. | Add an explicit transient-GATT-error predicate and immediately rethrow non-transient errors. | not-fixed | 1 |
| 115 | src/smartcube/attachment/mac-address.ts | 9 | Low | Logic & Correctness | Collapsing separators with `+` and filtering empty fields accepts malformed addresses such as `aa::bb:cc:dd:ee:ff` or `aa:bb:cc:dd:ee:ff:` despite the documented strict-validation contract. | Split without collapsing/filtering empty fields, or validate the complete trimmed string with an anchored MAC-address regex before parsing. | not-fixed | 1 |
| 116 | src/smartcube/attachment/mac-probe-qiyi.ts | 48 | High | Logic & Correctness | Any plausible QiYi packet marks the candidate MAC valid, but decryption uses a fixed protocol key rather than the candidate MAC. Unsolicited gyro/state traffic or a response to an earlier write can therefore validate the wrong candidate. | Require a CRC-valid, structurally valid hello response attributable to a successfully completed hello write for the current candidate; reject unrelated packet types. | partial | 1 |
| 117 | src/smartcube/attachment/mac-probe-qiyi.ts | 65 | High | Logic & Correctness | The 100 ms interval submits fire-and-forget writes faster than they may complete. The shared write queue retains already-enqueued writes after `stopped`, allowing old-candidate frames to execute during later probes and race `stopNotifications()`. | Use a sequential retry loop that awaits each write, checks cancellation between writes, and is fully awaited before stopping notifications or returning. | fixed | 1 |
| 118 | src/smartcube/attachment/mac-probe-qiyi.ts | 75 | Medium | Logic & Correctness | When `maxTimer` resolves the wait, the `poll` interval is never cleared. Every timed-out probe permanently leaves a 40 ms interval running. | Use one settlement/cleanup function that clears both timers on every exit, or replace polling with timeout and abort events. | fixed | 1 |
| 119 | src/smartcube/attachment/mac-probe-qiyi.ts | 57 | Medium | Logic & Correctness | Listener and notification cleanup is not protected by `try/finally`. If `startNotifications()` rejects after listener registration, the listener remains attached with live captured state; later failures can likewise bypass cleanup. | Wrap the notification lifecycle in `try/finally`, track whether notifications started, and always remove the listener, cancel timers/writes, and conditionally stop notifications. | fixed | 1 |
| 120 | src/smartcube/attachment/mac-probe-qiyi.ts | 65 | Medium | Shortcuts & Patches | Every write failure is silently swallowed, so transport failures are misreported as invalid MACs after a full timeout. Cleanup failures are also suppressed at line 88, potentially leaving notifications active without any diagnostic. | Retry only explicitly transient failures and propagate or record terminal write/cleanup errors separately from a negative candidate result. | not-fixed | 1 |
| 121 | src/smartcube/attachment/mac-probe-qiyi.ts | 24 | Medium | Logic & Correctness | `timeoutMs` and `AbortSignal` do not cover connection, service discovery, characteristic discovery, or notification startup. Cancellation is only polled after all setup and the first write, so an aborted probe can continue mutating the connection for an unbounded period. | Establish a deadline and abort handling at entry, check between awaited operations, and use an abort listener rather than delayed polling. | partial | 1 |
| 122 | src/smartcube/attachment/mac-probe-qiyi.ts | 13 | Low | Refactoring Debt | The 78-line function combines connection management, discovery, packet parsing, write retries, timeout/abort coordination, and cleanup, making lifecycle invariants difficult to enforce. | Extract packet validation and a cancellation-aware notification wait/retry helper, leaving the top-level function responsible for orchestration and guaranteed cleanup. | not-fixed | 1 |
| 123 | src/smartcube/attachment/moyu32-session-crypto.ts | 35 | Medium | Logic & Correctness | `decrypt` does not reject frames shorter than one AES block. Lengths 1–15 throw an `aes-js` implementation error, while an empty frame unexpectedly becomes the 16-byte IV because missing array elements are coerced to zero. | Validate `raw.length >= 16` and throw a consistent domain-specific error before processing. | fixed | 1 |
| 124 | src/smartcube/attachment/moyu32-session-crypto.ts | 42 | Medium | Logic & Correctness | `encrypt` silently expands inputs shorter than 16 bytes: the XOR loop writes through index 15 and treats missing elements as zero. This changes the frame length and encrypts implicit padding instead of rejecting malformed input. | Validate `data.length >= 16` before copying or XORing; add boundary tests for lengths 0, 1, 15, and 16. | fixed | 1 |
| 125 | src/smartcube/attachment/moyu32-session-crypto.ts | 43 | Low | Duplication | Head-block encryption and tail-block encryption repeat the same XOR, AES, and copy-back logic at lines 43–49 and 52–58. The corresponding decrypt path repeats much of the same block traversal. | Extract `encryptBlock(buffer, offset)` and `decryptBlock(buffer, offset)` helpers and call them in the required order. | fixed | 1 |
| 126 | src/smartcube/attachment/moyu32-session-crypto.ts | 15 | Low | Refactoring Debt | The 48-line factory combines key derivation, cipher construction, block traversal, and both transforms, exceeding the requested 30-line threshold and making boundary behavior harder to test independently. | Move key derivation and block transforms into focused helpers or a small session-crypto class. | partial | 1 |
| 127 | src/smartcube/attachment/moyu32-session-crypto.ts | 19 | Low | Shortcuts & Patches | Protocol geometry is encoded through repeated magic values (`6`, `5`, and numerous `16`s), obscuring the MAC-reversal and AES-block invariants. | Introduce named constants such as `MAC_BYTE_LENGTH` and `AES_BLOCK_SIZE`, and use `MAC_BYTE_LENGTH - 1 - i` for reversal. | fixed | 1 |
| 128 | src/smartcube/attachment/packet-sanity.ts | 7 | Low | Logic & Correctness | MoYu32 packets are accepted when longer than the protocol’s 20-byte frame, allowing malformed trailing data. | Require `bytes.length === 20`. | fixed | 1 |
| 129 | src/smartcube/attachment/packet-sanity.ts | 12 | Low | Logic & Correctness | Byte validation accepts `NaN`, fractional values, and sparse-array holes because only numeric bounds are checked. These values can corrupt bit offsets or be silently ignored. | Require every element to satisfy `Number.isInteger(value) && value >= 0 && value <= 255`. | partial | 1 |
| 130 | src/smartcube/attachment/packet-sanity.ts | 26 | Medium | Logic & Correctness | The entropy heuristic rejects genuine battery packets, which normally contain an opcode, battery value, and 18 zero bytes. Conversely, nearly constant packets containing values other than `0` or `255` pass. | Remove the generic entropy heuristic and validate each opcode’s actual field and reserved-byte structure. | partial | 1 |
| 131 | src/smartcube/attachment/packet-sanity.ts | 29 | Low | Duplication | Byte-to-bit conversion and protocol field parsing duplicate the production MoYu32 parser, creating two schemas that can drift. | Extract shared MoYu32 decoding/constants and apply separate semantic validation to the decoded structure. | partial | 1 |
| 132 | src/smartcube/attachment/packet-sanity.ts | 34 | Low | Dead Code | `expectedOpcode` is derived from the same bit string immediately before being passed, so the comparison at line 47 is tautological. | Remove the parameter and duplicate opcode parse, or pass an independently established expected opcode. | fixed | 1 |
| 133 | src/smartcube/attachment/packet-sanity.ts | 35 | Low | Shortcuts & Patches | Broad silent catches here and at lines 101 and 142 suppress programming defects even though malformed input should be handled through explicit validation. | Remove redundant catches or narrow them to genuinely expected conversion errors and preserve diagnostics for unexpected failures. | fixed | 1 |
| 134 | src/smartcube/attachment/packet-sanity.ts | 40 | Low | Refactoring Debt | The 65-line validator mixes dispatch with four packet schemas and contains multiple nested branches. | Dispatch by opcode to small validators such as `validateHardware`, `validateMove`, and `validateFacelets`. | fixed | 1 |
| 135 | src/smartcube/attachment/packet-sanity.ts | 48 | Medium | Logic & Correctness | Opcode `171` is accepted without any gyro-body validation; after the generic entropy check, execution reaches the unconditional `true` at line 100. | Validate the fixed-point quaternion bounds/norm and reserved bytes for gyro packets. | not-fixed | 1 |
| 136 | src/smartcube/attachment/packet-sanity.ts | 64 | Medium | Logic & Correctness | Move codes are counted independently of their slots, but timestamps are then read from the first `validMoveCount` slots. Interspersed filler codes therefore associate validation with the wrong timestamps. | Parse each code and timestamp as one slot; require valid moves followed by trailing fillers, or validate timestamps by their original slot indices. | fixed | 1 |
| 137 | src/smartcube/attachment/packet-sanity.ts | 93 | Medium | Logic & Correctness | Facelet validation checks only overall bit balance. Illegal 3-bit sticker values and impossible cube states readily pass, despite the production parser being able to reject them. | Decode the stickers and verify both color ranges/counts and `CubieCube.fromFacelet(...)` legality. | fixed | 1 |
| 138 | src/smartcube/attachment/packet-sanity.ts | 107 | Low | Refactoring Debt | The 39-line QiYi validator combines two unrelated wire formats and repeats production decoding logic. | Split framed-message and quaternion validation and share protocol constants/helpers with the production parser. | fixed | 1 |
| 139 | src/smartcube/attachment/packet-sanity.ts | 114 | High | Logic & Correctness | `payload[1]` is the declared frame length, not a command. The code neither checks it against `payload.length` nor verifies the frame’s CRC, so truncated or corrupt frames can be accepted. | Rename it to `frameLength`, validate opcode-specific lengths and `frameLength <= payload.length`, then require a valid CRC over exactly the declared frame. | partial | 1 |
| 140 | src/smartcube/attachment/packet-sanity.ts | 116 | High | Logic & Correctness | State-change opcode `3` is accepted as proof of a MAC candidate even though QiYi decryption uses a public fixed key and only the outgoing hello contains the candidate MAC. Unsolicited traffic therefore can validate a wrong candidate. | For MAC probing, accept only a CRC-valid hello response directly associated with the candidate handshake. | partial | 1 |
| 141 | src/smartcube/attachment/packet-sanity.ts | 121 | Low | Logic & Correctness | Valid unsigned timestamp boundary values `0` and `0xffffffff` are treated as invalid without protocol justification; either can occur at initialization or wraparound. | Rely on CRC and frame semantics, or document and test a protocol-defined timestamp restriction. | fixed | 1 |
| 142 | src/smartcube/attachment/packet-sanity.ts | 126 | High | Logic & Correctness | Any quaternion notification can validate a MAC candidate despite containing no candidate-dependent data. This can select an incorrect MAC when gyro traffic is already active. | Do not accept quaternion packets as MAC proof; require the candidate-specific hello response. | fixed | 1 |
| 143 | src/smartcube/attachment/packet-sanity.ts | 136 | Medium | Logic & Correctness | Quaternion validation checks only individual component magnitude. It accepts an all-zero quaternion, vectors with grossly invalid norm, and packets with invalid CRC bytes 14–15. | Verify the quaternion CRC and require the squared norm to be within a documented tolerance around the unit scale. | fixed | 1 |
| 144 | src/smartcube/attachment/qiyi-wire.ts | 17 | Medium | Logic & Correctness | CRC input values are neither validated nor constrained to bytes; negative, fractional, or >255 values produce non-standard or even negative CRC results. | Validate every value as an integer in `[0,255]`, or narrow the API to `Uint8Array`. | fixed | 1 |
| 145 | src/smartcube/attachment/qiyi-wire.ts | 25 | Low | Duplication | Encryption and decryption duplicate the same block-allocation and iteration logic, although `aes-js` ECB already processes complete multi-block buffers. | Call the ECB operation once per buffer, or introduce one shared block-transform helper with centralized length validation. | fixed | 1 |
| 146 | src/smartcube/attachment/qiyi-wire.ts | 45 | Medium | Logic & Correctness | The one-byte frame-length field is not range-checked. Content longer than 251 bytes makes `4 + content.length` wrap during `Uint8Array.from`, producing a frame whose declared length does not match its payload. Invalid content values are also silently narrowed only after the CRC was computed, yielding corrupt frames. | Reject content longer than 251 bytes and validate every element as an integer byte before constructing the message and calculating its CRC; preferably accept `Uint8Array`. | fixed | 1 |
| 147 | src/smartcube/attachment/qiyi-wire.ts | 56 | Low | Refactoring Debt | The hello command is an opaque positional sequence of magic bytes, making field changes and protocol review error-prone. | Represent the fixed portion as a named immutable protocol constant and document or name its fields and offsets. | fixed | 1 |
| 148 | src/smartcube/attachment/qiyi-wire.ts | 58 | Low | Logic & Correctness | Missing MAC octets are silently replaced with zero and extra octets are ignored, so malformed input produces a plausible but incorrect handshake instead of failing immediately. Current callers validate first, but this helper does not enforce its own invariant. | Require exactly six integer bytes in `[0,255]` and throw on malformed input; remove the `?? 0` fallback. | fixed | 1 |
| 149 | src/smartcube/attachment/resolve-mac.ts | 38 | Medium | Logic & Correctness | Both provider awaits ignore cancellation, so an aborted operation can remain pending indefinitely. | Pass the signal to providers and check `throwIfAborted` immediately before and after each await. | partial | 1 |
| 150 | src/smartcube/attachment/resolve-mac.ts | 64 | Medium | Logic & Correctness | Aborting candidate probing only breaks the loop; execution then invokes the final MAC provider at line 85 despite cancellation. | Throw `AbortError` immediately instead of breaking, or add `throwIfAborted` before the fallback provider. | fixed | 1 |
| 151 | src/smartcube/attachment/resolve-mac.ts | 78 | Medium | Shortcuts & Patches | The blanket catch silently treats every probe failure—including aborts, GATT disconnection, and programming errors—as an invalid candidate, masking the real failure and potentially repeating a broken operation. | Rethrow abort and infrastructure errors; continue only for an explicitly classified candidate mismatch. | partial | 1 |
| 152 | src/smartcube/attachment/resolve-mac.ts | 55 | Low | Duplication | Name-derived candidates are recomputed at line 62, creating duplicate work and potentially inconsistent results if the callback is stateful. | Compute the candidates once and reuse the array for direct selection and probing. | fixed | 1 |
| 153 | src/smartcube/attachment/resolve-mac.ts | 29 | Low | Refactoring Debt | The 64-line resolver combines source precedence, advertisement acquisition, candidate selection, probing, cancellation, and provider fallback, with probing nested four levels deep. | Extract candidate probing and provider resolution into focused helpers. | not-fixed | 1 |
| 154 | src/smartcube/ble-utils.ts | 28 | Medium | Logic & Correctness | Slicing `mfData.buffer` ignores the DataView’s `byteOffset` and `byteLength`, potentially reading unrelated bytes and returning an incorrect MAC for subviews | Construct the new view using `mfData.byteOffset + 2` and `mfData.byteLength - 2` | fixed | 1 |
| 155 | src/smartcube/ble-utils.ts | 30 | Low | Logic & Correctness | CIC lookup stops at the first present identifier even when its value is missing or shorter than six bytes, preventing fallback to later valid identifiers | Continue iterating until a valid DataView of at least six bytes is found | fixed | 1 |
| 156 | src/smartcube/ble-utils.ts | 41 | Medium | Logic & Correctness | `reversedByteOrder=false` still reverses bytes; it merely switches from the first six bytes to the last six, contradicting the parameter’s meaning and producing identical results for six-byte payloads | Separate byte-window selection from byte-order selection, or make `false` iterate in forward order | fixed | 1 |
| 157 | src/smartcube/ble-utils.ts | 41 | Low | Duplication | Both branches duplicate MAC-byte formatting and differ only in index calculation | Determine the byte indices/window first, then use one formatting loop | not-fixed | 1 |
| 158 | src/smartcube/ble-utils.ts | 18 | Low | Refactoring Debt | The 34-line function combines input normalization, CIC selection, payload-window selection, byte ordering, and formatting | Extract payload selection and MAC formatting into focused helpers | not-fixed | 1 |
| 159 | src/smartcube/connect.ts | 37 | Medium | Logic & Correctness | The hardware/battery fallback commands can never complete MAC verification because `waitForVerifiedCubeEvent` accepts only `FACELETS`; a MAC-bearing connection without facelet capability will always time out. | Require facelet capability and fail fast, or define valid proof predicates corresponding to each requested response type. | fixed | 1 |
| 160 | src/smartcube/connect.ts | 44 | Low | Shortcuts & Patches | The refresh command rejection is silently discarded, turning an immediate transport/write failure into a misleading 10-second MAC timeout. | Propagate the command rejection into verification while retaining an independent timeout for commands that remain pending. | partial | 1 |
| 161 | src/smartcube/connect.ts | 51 | Low | Refactoring Debt | The 65-line function manually manages subscription, timer, abort listener, settlement, and cleanup, increasing lifecycle complexity and contributing to the missing completion case. | Compose RxJS operators such as `take(1)`, `timeout`, and an abort observable, or extract a reusable abortable-observable helper. | not-fixed | 1 |
| 162 | src/smartcube/connect.ts | 106 | Medium | Logic & Correctness | The subscription has no `complete` handler. If the cube disconnects or `events$` completes before a valid facelet, verification remains pending until the timeout and reports an incorrect MAC-address error. | Reject immediately on completion with a connection-closed error and clear the timer and abort listener. | fixed | 1 |
| 163 | src/smartcube/connect.ts | 146 | Low | Refactoring Debt | `connectSmartCube` is a 92-line orchestration function combining selection, advertisement discovery, GATT discovery, protocol resolution, connection, verification, cache mutation, and cleanup. | Extract focused selection/discovery, protocol-connection, and MAC-verification functions. | not-fixed | 1 |
| 164 | src/smartcube/connect.ts | 150 | Low | Logic & Correctness | `getRegisteredProtocols()` returns the mutable registry array directly, so this function retains live shared state across multiple `await`s. Concurrent registration or mutation can make protocol resolution use a different set from the one used to build the device picker. | Snapshot it with `[...getRegisteredProtocols()]`, and preferably expose a readonly registry API. | fixed | 1 |
| 165 | src/smartcube/connect.ts | 162 | Medium | Logic & Correctness | Cancellation is checked only after `requestDevice`, so an already-aborted call still opens the Bluetooth picker. There is also no post-`protocol.connect` check before returning connections without a MAC, allowing an abort racing with connection completion to be ignored. | Check the signal before opening the picker and immediately after `protocol.connect`; disconnect the returned connection if that latter check fails. | fixed | 1 |
| 166 | src/smartcube/connect.ts | 173 | Low | Shortcuts & Patches | Advertisement timeouts are unexplained magic values selected through an unrelated boolean option, making timing policy difficult to understand, test, or tune. | Introduce named constants or explicit timeout options and document why address search changes the duration. | fixed | 1 |
| 167 | src/smartcube/connect.ts | 198 | Low | Duplication | The raw GATT disconnect-and-ignore-errors block is duplicated at lines 220–224. | Centralize safe cleanup in a helper, with separate support for pre-connection GATT cleanup and established connection cleanup. | fixed | 1 |
| 168 | src/smartcube/connect.ts | 206 | Medium | Logic & Correctness | `onStatus('Verifying connection…')` executes after a connection exists but outside the verification `try`. If the user callback throws, `connectSmartCube` rejects while leaving the connection active. | Isolate status callback failures or move the call into a scope that always disconnects the established connection on failure. | fixed | 1 |
| 169 | src/smartcube/connect.ts | 220 | Medium | Logic & Correctness | Verification failure disconnects raw GATT instead of calling `conn.disconnect()`, bypassing the connection abstraction’s notification shutdown, listener removal, timer cleanup, reference clearing, and deterministic stream completion. | Await `conn.disconnect()` in a guarded cleanup block, using raw GATT disconnection only as a fallback if connection cleanup fails. | fixed | 1 |
| 170 | src/smartcube/cubie-cube.ts | 104 | High | Logic & Correctness | `fromFacelet` never verifies that each corner and edge matched exactly once; balanced malformed input is accepted, while unmatched positions retain stale/default values | Parse into temporary arrays, require every slot to match uniquely, and commit only after complete validation | fixed | 1 |
| 171 | src/smartcube/cubie-cube.ts | 130 | High | Logic & Correctness | The parser does not enforce cube solvability invariants, so single flipped edges, twisted corners, and permutation-parity mismatches return success; callers incorrectly rely on this as a legal-state check | Validate edge orientation sum, corner orientation sum, cubie uniqueness, and matching corner/edge permutation parity before returning success | not-fixed | 1 |
| 172 | src/smartcube/cubie-cube.ts | 93 | Medium | Logic & Correctness | Input length is not validated: very short strings can throw when `centers` becomes `NaN`, while valid 54-character input with trailing garbage is silently accepted | Require `facelet.length === 54` before reading fixed indices and return `-1` consistently for invalid input | fixed | 1 |
| 173 | src/smartcube/cubie-cube.ts | 45 | Low | Logic & Correctness | Multiplication corrupts its inputs when `prod === a`, because results are written while later iterations still read from `a`; the API does not document or prevent this aliasing | Compute into temporary arrays or return a new `CubieCube`; alternatively enforce and document distinct operands | fixed | 1 |
| 174 | src/smartcube/cubie-cube.ts | 88 | Low | Refactoring Debt | `fromFacelet` combines color normalization, counting, corner decoding, edge decoding, mutation, and validation in one long routine with terse names and nested searches | Extract normalization and corner/edge parsing helpers with descriptive names and explicit result types | not-fixed | 1 |
| 175 | src/smartcube/cubie-cube.ts | 101 | Low | Shortcuts & Patches | The undocumented `0x999999` packed-count sentinel obscures the nine-stickers-per-color invariant and makes the validation difficult to audit | Maintain six explicit counters and require every count to equal nine | fixed | 1 |
| 176 | src/smartcube/event-bus.ts | 56,82 | Medium | Logic & Correctness | Synchronous subscriber reentrancy can reorder events and overwrite the snapshot before the original event reaches all subscribers, violating the documented callback invariant. | Serialize emissions with a processing flag and queue; defer nested emissions until the current state/live delivery completes. | fixed | 1 |
| 177 | src/smartcube/event-bus.ts | 27-28 | Medium | Logic & Correctness | The public observables expose the underlying `Subject` instances at runtime, allowing JavaScript consumers or casts to inject events, snapshots, or completion while bypassing all bus invariants. | Expose `this.live.asObservable()` and `this.state.asObservable()`. | fixed | 1 |
| 178 | src/smartcube/event-bus.ts | 25,94-105 | Medium | Logic & Correctness | A boolean force latch collapses concurrent battery requests into one forced emission. It can also remain armed after a request fails, causing an unrelated later reading to bypass deduplication. | Track pending requests individually and provide cancellation/rollback when a request fails. | partial | 1 |
| 179 | src/smartcube/event-bus.ts | 60,68-70 | Low | Logic & Correctness | Public `emit()` accepts `BATTERY` events that bypass clamping and do not update `lastBatteryLevel`; a subsequent identical `emitBattery()` therefore emits a duplicate despite the bus owning deduplication policy. | Route all battery events through one normalization/deduplication path or exclude `BATTERY` from `emit()`’s accepted type. | fixed | 1 |
| 180 | src/smartcube/event-bus.ts | 114,118 | Low | Logic & Correctness | `Partial<SmartCubeCapabilities>` permits explicitly undefined properties under the current TypeScript configuration; spreading such a patch produces capabilities whose runtime values violate their declared boolean types. | Reject non-boolean patch values at runtime and/or enable `exactOptionalPropertyTypes`. | fixed | 1 |
| 181 | src/smartcube/event-bus.ts | 118-123 | Low | Refactoring Debt | `setCapabilities()` publishes a new revision even when neither capabilities nor hardware changes, creating spurious state emissions and revisions. | Compare the computed state with the current state and return without publishing when unchanged. | partial | 1 |
| 182 | src/smartcube/event-bus.ts | 132-138 | Low | Duplication | `complete()` manually duplicates the snapshot reconstruction, revision increment, freezing, and publication already implemented by `publish()`. | Reuse `publish({ connected: false })` while retaining the existing connected-state guard. | fixed | 1 |
| 183 | src/smartcube/protocol.ts | 42 | Medium | Logic & Correctness | Returns the live registry array, allowing consumers to add, remove, or reorder protocols without validation. Mutation during `connectSmartCube()` can also alter driver selection across an `await`. | Return a readonly snapshot and provide explicit unregister/test-reset APIs. | fixed | 1 |
| 184 | src/smartcube/protocol.ts | 37 | Medium | Logic & Correctness | Stores the caller-owned mutable protocol object. Its filters, services, callbacks, and `needsMac` can change after registration, including during an active connection attempt. | Make protocol descriptors readonly and copy/freeze their arrays when registering. | partial | 1 |
| 185 | src/smartcube/protocol.ts | 36 | Low | Logic & Correctness | Duplicate detection uses object identity only. Equivalent protocol objects can be registered repeatedly, producing duplicate scan filters and order-dependent driver selection. | Give protocols stable IDs and store them in a keyed registry that rejects duplicate IDs. | not-fixed | 1 |
| 186 | src/smartcube/protocol.ts | 6 | Low | Logic & Correctness | The filter union is not mutually exclusive under TypeScript structural typing; an object may contain both `name` and `namePrefix`, after which the matcher silently ignores the prefix. | Use complementary `never` properties or an explicit discriminant, and validate external registrations. | fixed | 1 |
| 187 | src/smartcube/protocol.ts | 46 | Low | Logic & Correctness | The generated matcher closes over the mutable filter array, so later mutations silently change predicate behavior. | Accept `readonly SmartCubeNameFilter[]` and snapshot the filters when constructing the matcher. | partial | 1 |
| 188 | src/smartcube/protocol.ts | 48 | Low | Logic & Correctness | Normalizing a missing device name to `''` means an empty exact-name or prefix filter matches an unnamed device; empty prefixes additionally match every device. | Return `false` when `device.name` is absent and reject empty filter values during registration. | fixed | 1 |
| 189 | src/smartcube/protocols/gan.ts | 135 | Medium | Logic & Correctness | Capability reconciliation is restricted to Gen2, although the Gen3 driver emits `gyroSupported: false`; Gen3 therefore reports `capabilities.gyroscope === true` while its hardware snapshot says false. | Add Gen3-specific capability defaults or reconcile Gen3 hardware events while retaining Gen4’s lazy gyro-detection behavior. | fixed | 1 |
| 190 | src/smartcube/protocols/gan.ts | 213 | Medium | Logic & Correctness | A cached MAC is accepted without validation. A malformed cached value reaches key derivation and throws before verification can remove the cache, causing repeat connection failures. | Validate cached addresses before accepting them; remove invalid cache entries and continue through the resolution ladder. | fixed | 1 |
| 191 | src/smartcube/protocols/gan.ts | 214 | Medium | Logic & Correctness | Cancellation is checked only after multiple asynchronous MAC-resolution attempts. Aborting during either provider call can still trigger subsequent waits and another provider invocation. | Check `context.signal` after each awaited operation and before starting each fallback. | fixed | 1 |
| 192 | src/smartcube/protocols/gan.ts | 221 | Medium | Logic & Correctness | `waitForManufacturerData` is called without the available abort signal, so cancellation can be delayed by the full five-second timeout. | Pass `{ earlyExitOnEmptyFirstAdvertisement: false, signal: context?.signal }`. | fixed | 1 |
| 193 | src/smartcube/protocols/gan.ts | 209 | Medium | Duplication | The MAC-resolution ladder duplicates the existing shared `resolveCubeMac` abstraction and has already drifted from it by omitting cancellation support. | Replace lines 209–231 with `resolveCubeMac` configured for GAN manufacturer-data parsing. | fixed | 1 |
| 194 | src/smartcube/protocols/gan.ts | 197 | Low | Duplication | Primary services are rediscovered even when `context.serviceUuids` was already collected by the attachment pipeline. This adds a redundant BLE round trip and a second failure point. | Reuse `context.serviceUuids` when present; discover services only for direct calls without context. | fixed | 1 |
| 195 | src/smartcube/protocols/gan.ts | 237 | Low | Logic & Correctness | The externally owned browser `BluetoothDevice` is mutated with a nonstandard `mac` property, allowing stale connection-specific state to leak across attempts. | Store the MAC directly in the legacy connection instead of extending and mutating `BluetoothDevice`. | fixed | 1 |
| 196 | src/smartcube/protocols/gan.ts | 98 | Low | Dead Code | Initialization captures every event, but replay occurs before the connection is returned. Replayed MOVE/GYRO events have no subscribers and do not update snapshot state, so their capture and replay are ineffective. | Capture only cacheable events, or use an explicit buffered/replay mechanism if transient initialization events must reach consumers. | partial | 1 |
| 197 | src/smartcube/protocols/gan.ts | 186 | Low | Refactoring Debt | `connectGanDevice` is over 70 lines and combines GATT setup, generation detection, MAC resolution, legacy connection creation, and wrapper construction. | Extract GATT/profile discovery, MAC resolution, and generation-to-wrapper construction into focused helpers. | fixed | 1 |
| 198 | src/smartcube/protocols/gan.ts | 249 | Low | Refactoring Debt | The nested generation ternary silently treats every value other than `gen2` and `gen3` as Gen4, so adding a generation can compile into incorrect protocol metadata. | Use an exhaustive `Record<GanGeneration, SmartCubeProtocolInfo>` lookup or exhaustive switch. | fixed | 1 |
| 199 | src/smartcube/protocols/gan.ts | 288 | Low | Duplication | Gen2, Gen3, and Gen4 affinity checks are identical copy-paste branches. | Store normalized classic service UUIDs in a collection and use one membership check. | fixed | 1 |
| 200 | src/smartcube/protocols/gocube.ts | 54 | Low | Logic & Correctness | `parseInt` accepts malformed components such as `1junk` and `1.5`, producing a seemingly valid quaternion | Validate each trimmed component with a strict signed-integer pattern before conversion | not-fixed | 1 |
| 201 | src/smartcube/protocols/gocube.ts | 180 | Medium | Logic & Correctness | The frame’s declared length byte is never checked against the received byte length, so truncated or malformed frames can pass validation | Require byte 1 to equal `value.byteLength - 2` before dispatching the frame | partial | 1 |
| 202 | src/smartcube/protocols/gocube.ts | 189 | Medium | Logic & Correctness | Frames shorter than seven bytes bypass checksum validation entirely and can generate fabricated events, such as a battery event using the CR/checksum byte as its level | Reject frames below the minimum supported size and validate the checksum for every accepted frame | partial | 1 |
| 203 | src/smartcube/protocols/gocube.ts | 217 | High | Logic & Correctness | A truncated move frame fabricates an opposite-face inverse move from the previous move, despite containing insufficient evidence about the actual turn; this silently corrupts tracked cube state | Drop incomplete move frames and request a full state resynchronization instead of synthesizing a move | not-fixed | 1 |
| 204 | src/smartcube/protocols/gocube.ts | 228 | Medium | Logic & Correctness | Odd-length move payloads are accepted, causing the final unpaired byte to be decoded as another move | Require a nonzero, even move-payload length before iterating over byte pairs | fixed | 1 |
| 205 | src/smartcube/protocols/gocube.ts | 229 | Medium | Logic & Correctness | The face/direction byte is not range-checked; values above 11 produce an undefined axis and can throw inside `CubeMult` | Validate the raw move code before indexing `AXIS_PERM`, rejecting or resynchronizing on invalid values | fixed | 1 |
| 206 | src/smartcube/protocols/gocube.ts | 234 | High | Logic & Correctness | Cube-state messages have no minimum payload-length guard, yet the handler unconditionally reads 54 sticker bytes; a short checksummed frame causes an uncaught `DataView` bounds exception | Require at least 54 state payload bytes before decoding | fixed | 1 |
| 207 | src/smartcube/protocols/gocube.ts | 248 | Medium | Logic & Correctness | The return value of `fromFacelet` is ignored; when decoding fails, the buffers are still swapped and stale or partially initialized state becomes authoritative | Swap cubes only when `fromFacelet(newFacelet) !== -1`; otherwise discard the frame and request resynchronization | fixed | 1 |
| 208 | src/smartcube/protocols/gocube.ts | 265 | Medium | Logic & Correctness | Battery messages have no payload-length validation, allowing a missing payload to be interpreted as framing data | Require exactly one battery payload byte before emitting | partial | 1 |
| 209 | src/smartcube/protocols/gocube.ts | 279 | High | Logic & Correctness | A device disconnect during initialization completes the event bus but does not settle the initial-state wait; after five seconds `connect` resolves with an already-disconnected connection | Reject the initialization promise on disconnect and verify GATT is still connected before returning | partial | 1 |
| 210 | src/smartcube/protocols/gocube.ts | 279 | Low | Duplication | Natural and explicit disconnect paths duplicate teardown logic and have diverged: the natural path leaves characteristic listeners and references intact | Introduce one idempotent teardown routine used by both paths | fixed | 1 |
| 211 | src/smartcube/protocols/gocube.ts | 313 | High | Logic & Correctness | Initial-state timeout is treated as success; the subsequently emitted state is the default solved `CubieCube`, falsely reporting an unknown physical cube as solved | Reject on timeout, or leave facelets unknown and explicitly represent degraded initialization | fixed | 1 |
| 212 | src/smartcube/protocols/gocube.ts | 340 | Medium | Logic & Correctness | `REQUEST_FACELETS` emits cached state with a fresh timestamp before the device request succeeds, so consumers can mistake stale state for a fresh response and still receive it when the write fails | Send the request first and emit only the decoded response, preserving the cached snapshot’s original timestamp if fallback behavior is required | not-fixed | 1 |
| 213 | src/smartcube/protocols/gocube.ts | 403 | Medium | Logic & Correctness | The attachment context and its abort signal are ignored, so cancellation during GATT setup or the five-second state wait can still resolve successfully | Pass the context into initialization, race blocking operations against its signal, and clean up before throwing `AbortError` | partial | 1 |
| 214 | src/smartcube/protocols/gocube.ts | 180 | Low | Refactoring Debt | `parseData` is an approximately 90-line dispatcher combining framing, validation, decoding, state mutation, and event emission, which has contributed to inconsistent per-message validation | Extract a frame validator and dedicated move, state, orientation, and battery handlers | not-fixed | 1 |
| 215 | src/smartcube/protocols/moyu-mhc.ts | 245 | High | Logic & Correctness | Turn notifications begin before battery polling and initial cube-state synchronization finish. Moves during initialization are emitted before callers can subscribe and may be overwritten when the later snapshot is applied. | Queue turn packets until initialization completes and replay them against the synchronized state, or establish state before enabling turn notifications. | partial | 1 |
| 216 | src/smartcube/protocols/moyu-mhc.ts | 236 | High | Logic & Correctness | `MoyuV1Client` is created when only the write characteristic exists, although every command requires responses from the read characteristic. Such connections advertise unsupported capabilities, incur repeated timeouts, and never complete commands successfully. | Create the client only when both read and write characteristics exist and read notifications started successfully. | fixed | 1 |
| 217 | src/smartcube/protocols/moyu-mhc.ts | 139 | Medium | Logic & Correctness | The timestamp is assembled with signed JavaScript bitwise operations. When the high bit is set, the value becomes negative, producing negative cube timestamps after approximately half the 32-bit clock range. | Convert the assembled value to unsigned with `>>> 0` before scaling, and handle clock wrapping explicitly if timestamps span a rollover. | fixed | 1 |
| 218 | src/smartcube/protocols/moyu-mhc.ts | 119 | Medium | Logic & Correctness | An invalid device facelet payload silently resets internal state to solved; callers then emit that solved state as if it came from the device. This masks corruption and causes subsequent move reconstruction to start from false state. | Return a success result or throw on invalid facelets; preserve the last valid state and avoid emitting a fabricated solved snapshot. | fixed | 1 |
| 219 | src/smartcube/protocols/moyu-mhc.ts | 271 | Medium | Shortcuts & Patches | Any initial cube-state error—including timeout, malformed data, or protocol failure—is converted into a solved `FACELETS` event. “Unknown” and “solved” are materially different states. | Leave facelets unset, preserve prior state, or surface an initialization warning/error instead of reporting solved. | fixed | 1 |
| 220 | src/smartcube/protocols/moyu-mhc.ts | 259 | Medium | Logic & Correctness | Periodic battery polling and public `sendCommand()` calls are not serialized. Concurrent requests can interleave, including the multi-frame reset request, while `MoyuV1Client` uses a single response-fragment accumulator. | Route all v1 operations through one promise queue or mutex and prevent polling while another request is active. | partial | 1 |
| 221 | src/smartcube/protocols/moyu-mhc.ts | 225 | Medium | Logic & Correctness | Partial initialization has no rollback. If service discovery, notification startup, or initial requests fail, the disconnect listener, already-started notifications, and GATT connection remain active even though `connect()` rejects and no connection is returned for cleanup. | Wrap initialization in `try/catch` and invoke an idempotent cleanup routine before rethrowing. | fixed | 1 |
| 222 | src/smartcube/protocols/moyu-mhc.ts | 255 | Medium | Logic & Correctness | Initialization can succeed even when neither the turn characteristic nor a functional read/write control pair exists, returning a “connected” cube incapable of producing moves or servicing commands. | Validate that the discovered characteristic set provides at least one usable protocol path; otherwise clean up and reject connection. | fixed | 1 |
| 223 | src/smartcube/protocols/moyu-mhc.ts | 290 | Low | Logic & Correctness | Command event timestamps are captured before awaiting BLE responses, although `SmartCubeEvent.timestamp` is documented as the host time of emission. A slow response makes emitted facelet, battery, and hardware timestamps stale by up to the timeout duration. | Capture `now()` immediately before each emit, after the awaited response completes. | fixed | 1 |
| 224 | src/smartcube/protocols/moyu-mhc.ts | 324 | Low | Shortcuts & Patches | All command failures are swallowed, so callers receive a successfully resolved promise even when an advertised command timed out or failed. | Propagate explicit command failures; suppress errors only for autonomous best-effort polling. | fixed | 1 |
| 225 | src/smartcube/protocols/moyu-mhc.ts | 43 | Low | Logic & Correctness | `facelets` remains false for turn-only devices even though every recognized turn emits a `FACELETS` event. Capability metadata therefore contradicts observable behavior. | Define capability semantics consistently and set `facelets` when turn-based state reconstruction is available. | fixed | 1 |
| 226 | src/smartcube/protocols/moyu-mhc.ts | 32 | Low | Logic & Correctness | Zero, `NaN`, or infinite gyro components can produce a zero or non-finite quaternion, despite the normalization helper implying a valid unit quaternion. | Reject non-finite/near-zero samples or substitute a valid identity quaternion. | fixed | 1 |
| 227 | src/smartcube/protocols/moyu-mhc.ts | 310 | Low | Shortcuts & Patches | `bootCount` is exposed as `hardwareVersion`, producing a changing value that is not a hardware version. | Add an appropriate metadata field, use `hardwareName`, or omit the unsupported hardware version. | fixed | 1 |
| 228 | src/smartcube/protocols/moyu-mhc.ts | 203 | Low | Duplication | Natural and manual disconnect paths duplicate battery cleanup and bus completion, but only manual disconnect removes characteristic listeners and clears references. The paths have already drifted. | Extract an idempotent shared finalization routine, with optional notification stopping for manual disconnect. | fixed | 1 |
| 229 | src/smartcube/protocols/moyu-mhc.ts | 131 | Low | Refactoring Debt | `parseTurn()` is a long routine combining packet validation, timestamp decoding, rotation tracking, move derivation, cube mutation, and event publication. | Extract packet decoding and move-crossing calculation into small pure functions that can be tested independently. | not-fixed | 1 |
| 230 | src/smartcube/protocols/moyu-mhc.ts | 225 | Low | Refactoring Debt | `init()` spans discovery, client construction, three notification subscriptions, capability calculation, polling setup, state synchronization, and fallback policy, contributing directly to fragile partial-initialization behavior. | Split discovery, notification setup, state synchronization, and rollback into focused helpers. | not-fixed | 1 |
| 231 | src/smartcube/protocols/moyu-v1.ts | 21-22 | High | Logic & Correctness | The facelet map is not bijective: index 29 is assigned twice and index 15 is never assigned. Even solved stickers produce an invalid string containing `?` and a misplaced `F`. | Change the R-face entry `26` to `15` and the F-face entry `29` to `26`; add a test asserting the complete solved string equals `UUUUUUUUURRRRRRRRRFFFFFFFFFDDDDDDDDDLLLLLLLLLBBBBBBBBB`. | fixed | 1 |
| 232 | src/smartcube/protocols/moyu-v1.ts | 26,315 | Low | Logic & Correctness | The exported solved-state constant is a mutable nested array. External consumers can mutate it, permanently changing the default used by `setCubeState()`. | Expose a deeply readonly frozen value or return a fresh copy. | fixed | 1 |
| 233 | src/smartcube/protocols/moyu-v1.ts | 41-44 | Medium | Logic & Correctness | Notifications shorter than two bytes throw at `getUint8(1)`. The `Math.max` payload-length guard occurs too late to protect malformed frames. | Reject or ignore frames with `dv.byteLength < 2` before parsing either header byte. | fixed | 1 |
| 234 | src/smartcube/protocols/moyu-v1.ts | 60-72 | Low | Dead Code | `parseResponse()` accepts `timestamp` and returns it, but the caller never reads `r.timestamp`. | Remove the parameter and returned property, or use it consistently instead of separately carrying `receivedAt`. | fixed | 1 |
| 235 | src/smartcube/protocols/moyu-v1.ts | 82-96 | Low | Duplication | `IdGenerator` and `SendCountGenerator` are near-identical wrapping-counter classes differing only in initial value and modulus. | Replace them with one configurable counter abstraction or simple numeric fields plus a shared increment helper. | fixed | 1 |
| 236 | src/smartcube/protocols/moyu-v1.ts | 112-127 | Low | Shortcuts & Patches | Cube-state decoding assumes a 30-byte payload and otherwise exposes an incidental `RangeError` from `DataView`. | Validate `payload.byteLength >= 30` and throw a protocol-specific malformed-response error. | fixed | 1 |
| 237 | src/smartcube/protocols/moyu-v1.ts | 131-145 | Low | Logic & Correctness | The public encoder does not validate dimensions or values. Missing sticker rows throw a generic `TypeError`, while missing cells/angles can silently become zero through bitwise coercion. | Require exactly six 9-cell rows and six integer angles in the supported nibble range before encoding. | fixed | 1 |
| 238 | src/smartcube/protocols/moyu-v1.ts | 150-159 | Low | Logic & Correctness | Facelet conversion similarly trusts the nested array shape; missing cells silently map to sticker ID zero, and out-of-range values are changed by `& 15`. | Validate the 6×9 shape and sticker IDs before conversion instead of coercing malformed input. | partial | 1 |
| 239 | src/smartcube/protocols/moyu-v1.ts | 178-182 | Low | Shortcuts & Patches | The client has no close/dispose operation. Disconnecting leaves outstanding waiters and timers alive until their five-second timeout. | Add a disposal method that clears fragment state and rejects every waiter immediately, and invoke it during disconnect. | fixed | 1 |
| 240 | src/smartcube/protocols/moyu-v1.ts | 181,188-194 | High | Logic & Correctness | Fragment assembly uses one unkeyed array and treats arrival of the nominal final index as proof of completion. It accepts inconsistent totals, missing/duplicate indices, and mixed responses; a `total === 0` part is pushed and retained, poisoning the next response. | Track one validated assembly with expected total and sequence/counter, reject duplicates or gaps, clear invalid/stale assemblies, and merge only after every expected index exists. | partial | 1 |
| 241 | src/smartcube/protocols/moyu-v1.ts | 213-214,232 | Low | Logic & Correctness | Public `send()` accepts arbitrary commands, but `headerByte()` does not mask or validate them. Values outside 0–15 overwrite the payload and request-ID bits. | Require an integer command in `[0, 15]`, preferably through a command enum or restricted internal API. | fixed | 1 |
| 242 | src/smartcube/protocols/moyu-v1.ts | 217-228,258 | High | Logic & Correctness | Concurrent `send()` calls can interleave frames from different multipart requests. The shared GATT queue serializes individual writes, not complete requests. | Serialize the entire request/response transaction, or make multipart writes atomic under a client-level mutex. | fixed | 1 |
| 243 | src/smartcube/protocols/moyu-v1.ts | 218-224 | Medium | Logic & Correctness | Sixteen parts are allowed even though `total` occupies four bits. Encoding `16 << 4` into a byte produces a zero total, which the receiver path treats as invalid. | Cap requests at 15 parts, or explicitly implement the protocol’s zero-means-16 convention on both encoding and decoding if one exists. | fixed | 1 |
| 244 | src/smartcube/protocols/moyu-v1.ts | 232-268 | Low | Refactoring Debt | `send()` exceeds 30 lines and combines header construction, waiter allocation, timeout ownership, transport, and cleanup; the coupled responsibilities contribute directly to its race-prone lifecycle. | Extract request encoding and waiter registration/cleanup, then serialize them through a focused transaction method. | not-fixed | 1 |
| 245 | src/smartcube/protocols/moyu-v1.ts | 237-253 | Medium | Logic & Correctness | Request IDs wrap after eight sends without checking active waiters. Nine concurrent requests of the same command can share `(command,id)`, causing responses and timeout callbacks to remove or resolve the wrong waiter. | Allocate only currently unused IDs or enforce a single in-flight transaction. | fixed | 1 |
| 246 | src/smartcube/protocols/moyu-v1.ts | 245-267 | Medium | Logic & Correctness | The response promise can time out while `send()` is still awaiting a queued or hung GATT write. Because that promise is not returned or awaited yet, its rejection can be reported as unhandled, and the caller does not receive the timeout until the write finishes. | Make transport and response waiting part of one immediately observed promise/race, with unified cleanup on every outcome. | fixed | 1 |
| 247 | src/smartcube/protocols/moyu-v1.ts | 252,258 | Medium | Logic & Correctness | A notification may resolve the waiter after the device handles the write but before the `await sendRawRequest()` continuation assigns `sentAt`, returning the sentinel value `0`. | Record `sentAt` before transmission begins, or set it before the final write can produce a response. | fixed | 1 |
| 248 | src/smartcube/protocols/moyu-v1.ts | 288-297 | Low | Shortcuts & Patches | Battery decoding assumes an eight-byte response and otherwise leaks generic bounds errors. | Validate the response length before reading fields and reject with a descriptive protocol error. | fixed | 1 |
| 249 | src/smartcube/protocols/moyu-v1.ts | 303-309 | Low | Shortcuts & Patches | Hardware decoding assumes at least 24 bytes without validation. | Validate the minimum payload length before accessing offsets 16–23. | fixed | 1 |
| 250 | src/smartcube/protocols/moyu-v1.ts | 113-144,218-251 | Low | Shortcuts & Patches | Packet sizes, nibble masks, face counts, payload offsets, and timeout duration are scattered magic numbers, obscuring protocol constraints such as the 15-part limit. | Introduce named protocol-layout constants and derive bounds and offsets from them. | partial | 1 |
| 251 | src/smartcube/protocols/qiyi.ts | 43 | Medium | Logic & Correctness | The documented uint32 timestamp is decoded with signed bitwise arithmetic. Values become negative after `0x7fffffff`; sorting, `moveTs > lastTs`, and emitted `cubeTimestamp` then fail across the sign boundary and uint32 wraparound. | Decode with `>>> 0` or `DataView.getUint32`, then compare and order timestamps using wrap-aware modular arithmetic. | partial | 1 |
| 252 | src/smartcube/protocols/qiyi.ts | 218 | High | Logic & Correctness | State-change packets contain an authoritative facelet snapshot in bytes 7–33, but the implementation ignores it and derives state solely by applying the limited move history to `prevCubie`. Missing more history than the packet retains, an invalid move code, or packet loss permanently desynchronizes emitted facelets despite the correct state being available. | Parse and validate the packet facelets, reconcile recovered moves against them, and set `prevCubie` to the authoritative state after each packet. | fixed | 1 |
| 253 | src/smartcube/protocols/qiyi.ts | 285 | Medium | Logic & Correctness | `init()` is not exception-safe. Failures during service discovery, notification startup, or the hello write leave the disconnect listener, characteristic listener, notifications, and possibly the GATT connection active while no connection object is returned. | Wrap initialization in `try/catch` and run idempotent teardown before rethrowing. | fixed | 1 |
| 254 | src/smartcube/protocols/qiyi.ts | 278 | Medium | Duplication | Remote and explicit disconnect paths duplicate teardown logic but have diverged: `onDisconnect` does not remove the characteristic listener or clear `cubeChrct`, leaving stale connection state and causing later commands to write through a disconnected characteristic. | Centralize both paths in one idempotent teardown method that clears listeners, characteristic state, pending operations, and the event bus. | fixed | 1 |
| 255 | src/smartcube/protocols/qiyi.ts | 113 | Medium | Logic & Correctness | Queued writes capture the characteristic and remain runnable after `disconnect()` clears it; disconnect also starts notification teardown without waiting for `writeChain`. Concurrent commands or ACKs can therefore execute during or after teardown and collide with GATT operations. | Add a closed state checked inside each queued operation and serialize teardown behind, or explicitly settle/cancel, the write chain. | fixed | 1 |
| 256 | src/smartcube/protocols/qiyi.ts | 303 | Low | Logic & Correctness | `REQUEST_BATTERY` mutates battery-deduplication state before the hello write succeeds. If the write rejects or never produces a response, an unrelated later packet is incorrectly treated as the forced response. | Arm the forced emission only after a successful request, and clear it on failure or timeout. | partial | 1 |
| 257 | src/smartcube/protocols/qiyi.ts | 195 | Low | Shortcuts & Patches | Protocol-critical ACK failures are silently discarded here and again at line 215, so write failures can leave synchronization broken while the connection continues reporting itself as healthy. | Route ACKs through a shared helper that records or surfaces failure and transitions the connection to an appropriate failed/disconnected state. | not-fixed | 1 |
| 258 | src/smartcube/protocols/qiyi.ts | 190 | Low | Shortcuts & Patches | The parser reads timestamp and opcode-specific offsets after only a generic three-byte/CRC check. Unlike sync confirmation, hello and state-change frames have no schema-length validation, allowing short CRC-valid frames to trigger malformed ACKs and reads of absent fields. | Validate the declared length and each opcode’s minimum or exact frame size before reading fields or sending ACKs. | fixed | 1 |
| 259 | src/smartcube/protocols/qiyi.ts | 186 | Low | Refactoring Debt | `parseCubeData()` is roughly 90 lines and combines framing assumptions, three opcode handlers, history recovery, move decoding, cube mutation, ACKs, and event emission. Its positional constants and inline lookup arrays make protocol changes difficult to verify. | Split it into typed per-opcode handlers and extract packet offsets, move-code mappings, validation, and ACK construction into named helpers/constants. | partial | 1 |
| 260 | src/smartcube/types.ts | 4 | Medium | Refactoring Debt | Exported types named `SmartCube*Event` omit the mandatory `timestamp`, forcing consumers to intersect them manually and contradicting their names | Rename these to `*EventMessage`, then export timestamped variants or define each complete event directly | not-fixed | 1 |
| 261 | src/smartcube/types.ts | 4 | Medium | Logic & Correctness | Event payloads are mutable, so one RxJS subscriber can modify the shared event object before later subscribers receive it; nested gyro objects are mutable too | Make event fields deeply readonly and freeze emitted events at runtime | not-fixed | 1 |
| 262 | src/smartcube/types.ts | 7 | Medium | Logic & Correctness | `face`, `direction`, and `move` use unrestricted `number`/`string` types, permitting invalid values and mutually inconsistent combinations such as face U with move `"R'"` | Model faces and directions with literal unions and define a correlated move union or construct events through a validating factory | not-fixed | 1 |
| 263 | src/smartcube/types.ts | 44 | Low | Duplication | Quaternion and velocity repeat the same inline three-dimensional vector structure | Extract reusable `Vector3` and `Quaternion` types | fixed | 1 |
| 264 | src/smartcube/types.ts | 54 | Medium | Logic & Correctness | `SmartCubeProtocolInfo` fields are mutable; implementations expose shared protocol constant objects, so a consumer can corrupt metadata seen by other connections | Mark `id` and `name` readonly and freeze or copy protocol metadata at runtime | partial | 1 |
| 265 | src/smartcube/types.ts | 119 | Medium | Logic & Correctness | `capabilities` is typed as mutable although implementations return a frozen object; valid-looking assignments can therefore throw or silently fail | Expose `Readonly<SmartCubeCapabilities>` and use a separate mutable/internal construction type if needed | fixed | 1 |
| 266 | src/smartcube/types.ts | 121 | Low | Logic & Correctness | `events$` and `state$` are not readonly in the public interface, allowing consumers to replace the stream references despite implementations declaring them readonly | Add `readonly` to both properties | fixed | 1 |
| 267 | src/smartcube/types.ts | 130 | Low | Refactoring Debt | The exported provider uses an undocumented optional boolean flag, creating three apparent states and obscuring the two invocation phases | Make the phase explicit with a required discriminated value such as `{ phase: 'initial' \| 'fallback' }` and document its call contract | partial | 1 |
| 268 | src/utils.ts | 10 | Low | Logic & Correctness | Monotonic-clock detection only checks `window`; workers with `globalThis.performance` fall back to non-monotonic `Date.now()` with a different time origin. | Detect and bind `globalThis.performance.now()`. | fixed | 1 |
| 269 | src/utils.ts | 34 | Medium | Logic & Correctness | The raw-sum regression formula suffers catastrophic cancellation for large timestamps; `varX` can become zero or negative and silently force an incorrect slope of 1. | Compute covariance and variance from mean-centered values or use an online stable algorithm. | fixed | 1 |
| 270 | src/utils.ts | 36 | Medium | Logic & Correctness | Zero, one, or constant-X valid samples are treated as a valid slope of 1, causing `cubeTimestampCalcSkew` to falsely report 0% skew when skew is not estimable. | Require at least two finite, distinct-X samples and return an explicit unavailable result or throw. | partial | 1 |
| 271 | src/utils.ts | 52 | Medium | Shortcuts & Patches | Missing timestamps are fabricated using a hardcoded 50 ms interval; for gaps bounded by known timestamps, reverse-pass ordering clusters missing moves near the later timestamp instead of interpolating the gap. | Interpolate bounded gaps between known timestamps and define/configure a named fallback interval only for unbounded gaps. | fixed | 1 |
| 272 | src/utils.ts | 52 | Low | Duplication | The forward and reverse missing-timestamp passes duplicate the same neighbor-check-and-adjust logic. | Extract a directional helper or process contiguous missing ranges in one pass. | fixed | 1 |
| 273 | src/utils.ts | 68 | Medium | Logic & Correctness | Cube timestamps are regressed without unwrapping clock rollover; a 32-bit millisecond clock crossing its boundary produces a large backward jump and corrupts the fit/skew. | Unwrap timestamps monotonically using the protocol’s clock modulus before regression. | not-fixed | 1 |
| 274 | src/utils.ts | 143 | Medium | Logic & Correctness | `toKociembaFacelets` assumes valid lengths, integer ranges, permutations, and orientations. Malformed input can throw an opaque `TypeError` or silently return an invalid facelet string. | Validate lengths, ranges, uniqueness, orientation sums, and permutation parity before indexing; return or throw a clear validation error. | partial | 1 |
| 275 | src/utils.ts | 46 | Low | Refactoring Debt | The function spans 35 lines and combines cloning, heuristic timestamp recovery, regression, normalization, and object reconstruction. | Extract timestamp recovery and fitting into independently testable helpers, then map results with `{ ...move, cubeTimestamp }`. | not-fixed | 1 |
| 276 | tools/fixture-recorder.js | 18 | Low | Duplication | The `DataView` branch duplicates the generic `ArrayBufferView` conversion on line 20. | Use `ArrayBuffer.isView(source)` for all views and a single conversion path. | fixed | 1 |
| 277 | tools/fixture-recorder.js | 27 | Medium | Logic & Correctness | Reloading the script captures already-patched methods as “originals”; `restore()` then leaves older wrappers installed and hidden recorder closures active. | Detect an existing installation or store native methods under a stable `Symbol` and restore them before reinstalling. | fixed | 1 |
| 278 | tools/fixture-recorder.js | 42 | Low | Duplication | The two write wrappers are copy-paste implementations differing only in the original method selected. | Extract a helper that installs a write wrapper for a method/original pair. | fixed | 1 |
| 279 | tools/fixture-recorder.js | 43 | Medium | Logic & Correctness | Writes are recorded before the native operation is invoked or succeeds, so synchronous failures and rejected writes remain as successful replay traffic. | Track the pending entry and remove or explicitly mark it when the native operation fails without swallowing the error. | fixed | 1 |
| 280 | tools/fixture-recorder.js | 57 | Medium | Logic & Correctness | The notification listener is attached only after `startNotifications()` resolves, leaving a window in which an initial notification can be missed. | Attach the listener before starting notifications and remove it if startup rejects. | fixed | 1 |
| 281 | tools/fixture-recorder.js | 57 | Medium | Logic & Correctness | Every successful `startNotifications()` call adds another anonymous listener, causing duplicate entries when the method is called repeatedly. | Keep one named handler per characteristic in a `WeakMap` and avoid registering duplicates. | fixed | 1 |
| 282 | tools/fixture-recorder.js | 107 | Low | Logic & Correctness | The blob URL is revoked immediately after the synthetic click; browsers that consume the download asynchronously can observe an invalid URL. | Revoke the URL after the download has been queued, such as in a deferred callback, and remove the temporary anchor afterward. | fixed | 1 |
| 283 | tools/fixture-recorder.js | 109 | Medium | Shortcuts & Patches | `restore()` restores prototype methods but leaves recorder notification listeners active, so `traffic` continues changing and closures remain retained after restoration. | Track all installed listeners and remove them during `restore()`. | fixed | 1 |
| 284 | src/smartcube/index.ts | 37 | Medium | Logic & Correctness | Publicly exports `getRegisteredProtocols()`, which exposes the live mutable registry array; consumers can clear, reorder, or inject entries without validation, breaking protocol selection and causing `connectSmartCube()` failures | Return a readonly snapshot from the getter and keep registry mutation behind explicit registration/unregistration APIs | fixed | 1 |
| 285 | src/smartcube/index.ts | 3 | Low | Refactoring Debt | Built-in protocol initialization relies on hidden import-time self-registration across six modules; registry contents depend on module evaluation state and adding a driver requires updating both its module and this separate import list | Export protocol objects and register a centralized `BUILT_IN_PROTOCOLS` collection explicitly during initialization | not-fixed | 1 |
| 286 | src/smartcube/protocols/giiker.ts | 69 | Medium | Logic & Correctness | Direction mapping contradicts its documentation: direction `9`, documented as a half-turn, is normalized to `3` and emitted as a prime turn; unknown direction nibbles silently become clockwise moves. | Use an explicit validated mapping such as `{1: "", 2: "2", 3: "'", 9: "2"}` and reject unknown values. | fixed | 1 |
| 287 | src/smartcube/protocols/giiker.ts | 75 | Medium | Logic & Correctness | Full-length but corrupt frames are decoded without validating cubie IDs, permutations, or orientations. Invalid edge nibbles can throw inside `toFaceCube`; other corruption can emit impossible states. | Validate decoded coordinates and cube legality before conversion, and discard malformed notifications without mutating state. | partial | 1 |
| 288 | src/smartcube/protocols/giiker.ts | 94 | Medium | Logic & Correctness | Only byte 16 is decoded even though Giiker frames contain additional previous-move entries—byte 17 remains usable even in encrypted frames. Coalesced or dropped notifications therefore lose moves while FACELETS jumps ahead. | Decode the available history and reconcile it against `lastFacelet` to recover the missing ordered moves. | not-fixed | 1 |
| 289 | src/smartcube/protocols/giiker.ts | 36 | Low | Dead Code | `GiikerHexPayload.logicalBytes` is returned but no caller reads it; only the local loop needs the value. | Return `valhex` directly or omit `logicalBytes` from the returned object. | fixed | 1 |
| 290 | src/smartcube/protocols/giiker.ts | 112 | Medium | Logic & Correctness | `battery` and `reset` capabilities are always advertised even when the optional RW service or required characteristics are absent or setup fails; the corresponding commands then silently do nothing. | Initialize these capabilities as false and enable each only after its required characteristic path is successfully established. | fixed | 1 |
| 291 | src/smartcube/protocols/giiker.ts | 153 | Low | Logic & Correctness | Queued notifications retain the frame but not its receipt time, so `handleStateValue()` later assigns drain-time timestamps rather than the required host receipt time. | Queue `{value, receivedAt}` and pass the captured timestamp into the handler. | fixed | 1 |
| 292 | src/smartcube/protocols/giiker.ts | 198 | Medium | Duplication | Remote and explicit disconnects implement separate, divergent teardown paths. Remote disconnect leaves data listeners and characteristic references—including `rwWriteChrct`—intact, allowing later commands to target stale GATT objects. | Centralize teardown in one idempotent helper used by both paths and clear every listener, timer, queue, and characteristic reference. | partial | 1 |
| 293 | src/smartcube/protocols/giiker.ts | 216 | Medium | Logic & Correctness | Initialization has no failure cleanup. Errors after registering listeners or starting notifications reject `connect()` while leaving listeners and partially initialized resources attached. | Wrap initialization in `try/catch`, invoke centralized cleanup, disconnect GATT, then rethrow. | fixed | 1 |
| 294 | src/smartcube/protocols/giiker.ts | 225 | High | Logic & Correctness | Notifications received before `readValue()` are replayed after the read-derived state. If the read reflects a newer cube state, processing older queued frames afterward reverses state, emits phantom moves, and leaves a stale snapshot. | Establish one ordered initialization source—such as the first notification with a read fallback—and never replay pre-read frames after a newer baseline. | fixed | 1 |
| 295 | src/smartcube/protocols/giiker.ts | 240 | Medium | Shortcuts & Patches | Optional battery initialization mutates instance fields and installs a listener before all setup succeeds, while the blanket `catch` neither rolls back partial state nor distinguishes an absent service from an operational error. | Build with local variables, commit fields only after success, clean up partial setup on failure, and handle expected “service unavailable” errors explicitly. | partial | 1 |
| 296 | src/smartcube/protocols/giiker.ts | 249 | Medium | Logic & Correctness | The battery listener assumes every RW notification has at least two bytes and is a battery response. Short payloads throw `RangeError`, while unrelated responses can be emitted as battery values. | Require the expected minimum length and response opcode before reading byte 1. | fixed | 1 |
| 297 | src/smartcube/protocols/giiker.ts | 256 | Low | Shortcuts & Patches | Periodic battery write failures are swallowed completely, so a permanently broken control channel continues polling forever with no capability correction or diagnostic. | Track repeated failures, disable polling/update capabilities when the channel is unusable, and expose or log actionable errors. | partial | 1 |
| 298 | src/smartcube/protocols/giiker.ts | 261 | High | Logic & Correctness | A remote disconnect or other GATT failure during optional battery setup is swallowed as “battery unavailable”; initialization then sets `isReady` and returns a completed, disconnected connection. | Track connection lifecycle explicitly and abort initialization whenever GATT disconnects, regardless of whether the current operation is optional. | fixed | 1 |
| 299 | src/smartcube/protocols/giiker.ts | 266 | Medium | Logic & Correctness | Queued MOVE events are emitted synchronously before `connect()` returns, when consumers cannot yet subscribe to the live-only `events$`; those moves are permanently lost despite being queued. | Preserve pending moves until the connection is observable, or consume pre-connect frames solely as initialization state without pretending to publish live moves. | fixed | 1 |
| 300 | src/smartcube/protocols/giiker.ts | 278 | Low | Logic & Correctness | `forceNextBattery()` is set before the write; when the write rejects, the flag remains armed and an unrelated later poll is incorrectly forced through deduplication. | Clear the force flag on failure or arm it only once a request is successfully sent. | fixed | 1 |
| 301 | src/smartcube/protocols/giiker.ts | 215 | Medium | Refactoring Debt | `init()` exceeds 50 lines and mixes data-stream initialization, initial-state decoding, battery discovery, polling, error policy, and queued-event draining. This concentration contributes directly to the ordering and cleanup bugs. | Split it into ordered state initialization, optional control-service setup, and queue/lifecycle helpers. | fixed | 1 |
| 302 | src/smartcube/protocols/giiker.ts | 256 | Low | Duplication | Battery-request encoding and write behavior are duplicated between the timer callback and `sendCommand()`. | Introduce a named battery opcode and a shared `requestBattery()` helper. | partial | 1 |
| 303 | src/smartcube/protocols/giiker.ts | 349 | Low | Refactoring Debt | Device-name classification uses a six-level nested ternary, making precedence and future model additions unnecessarily error-prone. | Replace it with an ordered table or a small classifier using straightforward conditionals. | fixed | 1 |
| 304 | src/smartcube/protocols/moyu32.ts | 43 | Medium | Logic & Correctness | Manufacturer-data map values already exclude the company identifier, but every payload of at least eight bytes skips two bytes, potentially deriving the wrong MAC. | Distinguish raw `DataView` input from `BluetoothManufacturerData` values and skip the company ID only for raw advertisement blobs. | not-fixed | 1 |
| 305 | src/smartcube/protocols/moyu32.ts | 61 | Medium | Logic & Correctness | The first manufacturer entry containing six bytes is accepted regardless of company ID, so unrelated manufacturer data can produce a plausible but incorrect MAC and encryption key. | Restrict lookup to the known MoYu company identifiers and validate the payload layout. | not-fixed | 1 |
| 306 | src/smartcube/protocols/moyu32.ts | 110 | Low | Dead Code | `latestFacelet` is always overwritten immediately before being read and provides no persistent behavior beyond the event bus snapshot. | Remove the field and emit the local `facelet` value directly. | fixed | 1 |
| 307 | src/smartcube/protocols/moyu32.ts | 133 | Medium | Logic & Correctness | Requests made without a write characteristic silently resolve successfully, including commands issued after `disconnect()`. | Reject with a clear “not connected” error when `writeChrct` is null. | fixed | 1 |
| 308 | src/smartcube/protocols/moyu32.ts | 150 | Medium | Logic & Correctness | The interval callback discards the battery request promise without handling rejection; failed GATT writes can become unhandled promise rejections. | Attach `.catch()` or make the polling method handle expected write failures internally. | fixed | 1 |
| 309 | src/smartcube/protocols/moyu32.ts | 154 | Medium | Logic & Correctness | Notifications are decrypted and parsed without enforcing the 20-byte frame length or validating packet sanity. Truncated frames can throw from AES/DataView access, while short move frames can produce `NaN` and poison counter state. | Reject non-20-byte frames before decryption, validate opcode-specific fields, and contain parsing failures inside the notification handler. | partial | 1 |
| 310 | src/smartcube/protocols/moyu32.ts | 154 | Medium | Refactoring Debt | `parseData` is a roughly 119-line dispatcher containing hardware, facelet, battery, move-history, timestamp, and cube-state logic with substantial nesting. | Extract one handler per opcode plus reusable bit/byte field readers. | fixed | 1 |
| 311 | src/smartcube/protocols/moyu32.ts | 174 | Low | Logic & Correctness | Hardware names are decoded as all eight bytes and `trim()` does not remove NUL padding, despite NUL being accepted by the protocol sanity rules. | Stop decoding at the first NUL or explicitly strip trailing NUL bytes. | fixed | 1 |
| 312 | src/smartcube/protocols/moyu32.ts | 196 | Low | Dead Code | Assigning the facelet sequence to `this.moveCnt` has no effect because every move packet overwrites `moveCnt` before reading it. | Remove this assignment and make the current move counter a local variable in the move handler. | fixed | 1 |
| 313 | src/smartcube/protocols/moyu32.ts | 208 | Medium | Logic & Correctness | When a move arrives before a legal facelet packet, `prevMoveCnt === -1` causes a return without initializing the counter; every subsequent move is discarded until a facelet response happens to arrive. | Initialize `prevMoveCnt` from the first move packet and request/resynchronize facelets before processing later deltas. | partial | 1 |
| 314 | src/smartcube/protocols/moyu32.ts | 213 | Medium | Logic & Correctness | All five history entries must be valid even when only the newest `moveDiff` entries are needed. A filler or corrupt unused slot delays or discards otherwise valid current moves. | Calculate the counter delta first and validate only the history entries required for that delta. | fixed | 1 |
| 315 | src/smartcube/protocols/moyu32.ts | 218 | Low | Dead Code | Assigning `"U "` to an invalid history entry is unreachable in effect because `invalidMove` prevents the array from ever being consumed. | Remove the assignment and reject only the affected required entry. | fixed | 1 |
| 316 | src/smartcube/protocols/moyu32.ts | 297 | Low | Logic & Correctness | Remote-disconnect cleanup leaves the characteristic notification listener and characteristic/encrypter references intact, unlike explicit `disconnect()`. | Centralize teardown and use it for both remote and explicit disconnection. | fixed | 1 |
| 317 | src/smartcube/protocols/moyu32.ts | 308 | Medium | Shortcuts & Patches | Partial `init()` failures do not perform local rollback. Direct callers can be left with connected GATT resources, listeners, notifications, or an active interval. | Wrap initialization in `try/catch`, invoke shared teardown on failure, then rethrow. | fixed | 1 |
| 318 | src/smartcube/protocols/moyu32.ts | 320 | Medium | Logic & Correctness | Notifications are enabled before `encrypter` is initialized, so notifications delivered during `startNotifications()` are silently dropped by `onStateChanged`. | Create session crypto before registering/enabling notifications. | fixed | 1 |
| 319 | src/smartcube/protocols/moyu32.ts | 326 | Low | Duplication | The identical hardware/facelets/battery startup burst is written twice, duplicating the same sequence also conceptually present in the MAC probe. | Extract a named `sendInitBurst()` helper, ideally shared with the probe implementation. | not-fixed | 1 |
| 320 | src/smartcube/protocols/moyu32.ts | 350 | Low | Logic & Correctness | `forceNextBattery()` mutates deduplication state before the request succeeds; a failed write or an earlier outstanding battery response can consume or strand the forced-emission flag. | Associate forcing with the successful request/response or roll back the flag when the write fails. | partial | 1 |
| 321 | src/smartcube/protocols/moyu32.ts | 400 | Medium | Logic & Correctness | `namePrefix: '^S'` is treated literally by both Web Bluetooth and `deviceNameMatchesFilters`; it does not mean “starts with S,” so those devices cannot match. | Use `{ namePrefix: 'S' }` if that was intended, or remove the invalid filter. | fixed | 1 |
| 322 | src/smartcube/protocols/moyu32.ts | 400 | Low | Duplication | `WCU_MY3` is fully subsumed by the preceding `WCU_` prefix and can never expand matching behavior. | Remove the redundant narrower filter or replace `WCU_` with deliberately enumerated model prefixes. | fixed | 1 |
| 323 | src/smartcube/protocols/moyu32.ts | 402 | Medium | Shortcuts & Patches | The protocol depends on manufacturer data to discover its MAC but declares no `optionalManufacturerData`; access currently depends accidentally on another registered protocol requesting the same company ID. | Declare MoYu’s supported company identifiers on `moyu32Protocol` explicitly. | not-fixed | 1 |
